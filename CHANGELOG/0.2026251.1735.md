# 0.2026251.1735 — feat(spec): provider-class vocabulary single source + CandyKindCarrier (F3.1/F4.2 parser consolidation)

## Summary

Cutover F parser consolidation (F3.1 + F4.2 spec-side) — the spec repo leg of the wave.

**F4.2 — provider-class vocabulary held twice → ONE source in spec, CUE-derived; both sides derive from it.**

Duplication evidence (current tree):
- `charly/provider.go:78-95` — `providerClasses` 11-entry closed map (`kind|deploy|verb|step|build|builder|command|loader|refs|agent-runtime|terminal`)
- `plugin-box/candy/plugin-box/validate_rules.go:150-170` — `validPluginClasses` byte-identical 11-entry map, comment admits "mirrors core providerClasses"
- `spec/schema/candy.cue:525` — the `#PluginCapability` regex already carried the same 11 classes inline (a third copy)

Consolidation:
- `#ProviderClassNames: [...]` is now the ONE hand-maintained CUE list.
- `#PluginCapability`'s regex is DERIVED from that list (`strings.Join` into `#ProviderClassPattern`, referenced by `=~#ProviderClassPattern`) — CUE evaluates it concretely; the alternation can no longer drift from the list.
- schemagen `-mode=vocab` emits `spec.ProviderClasses` from the same list via a new `listValues` helper (sibling of `enumValues`).
- charly/provider.go and plugin-box/validate_rules.go derive their closed sets from `spec.ProviderClasses` in their companion PRs.

**F3.1 — the `"candy"` kind-word literal in the kind-blind dispatch → a word-keyed capability carrier.**

Duplication evidence:
- `charly/provider_kind_invoke.go:56` — `if pn.Disc == "candy"` in `runPluginKind`
- `charly/materialize.go:70` — same literal in `materializeDiscoveredNode`
- `charly/no_kind_switch_in_kernel_test.go:323-327` — the gate test's negative control that BLESSES the literal

Consolidation: `spec.CandyKindCarrier` (`IsCandyKind() bool`) — the same capability-carrier pattern as `StructuralKindCarrier`/`ValidatingKindCarrier` already in the dispatch. The kernel asks the PROVIDER, never names the word; the gate carve-out is deleted in the charly companion PR.

## How tested

```
$ cd spec && task cue:gen
cue:gen: regenerated spec/{cue_types_gen.go,vocab_gen.go,version_gen.go}
$ gofmt -l . && go build ./... && go vet ./...
(empty, exit 0)
$ go test ./... | tail -5
ok  github.com/opencharly/spec/refs 15.769s
ok  github.com/opencharly/spec/schema 1.105s
ok  github.com/opencharly/spec/spec 0.371s
ok  github.com/opencharly/spec/transport 0.181s
TEST_EXIT=0
$ go test -run TestGenReproducible ./spec/ -v
--- PASS: TestGenReproducible (0.22s)
```

Regeneration is reproducible (TestGenReproducible PASS on the regenerated tree).

## Rulebook compliance

- R3: one canonical implementation — the class vocabulary now exists once in CUE; the Go maps and the regex derive from it.
- R5: the `#PluginCapability` inline alternation (a third copy) is deleted in the same change.
- Boundary law F3.1: the kernel's candy routing becomes a provider-carrier ask (companion charly PR consumes it).

## Change classification

- Change class: refactor (parser consolidation, no behavior change — the derived regex produces byte-identical alternation)
- Verification gate: `task cue:gen` + full `go test ./...` on the final tree (parity/round-trip suites green)
- Attribution tier: `analysed on a live system` — full test suite ran live on the changed tree; the F-wave bed re-runs on the consolidated binary remain scheduled for the F6 wave step.

*Assisted-by: pi anthropic/claude-sonnet-4-20250514 (analysed on a live system)*
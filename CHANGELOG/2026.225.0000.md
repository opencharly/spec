# 2026.225.0000 — the packaging contract completion, made additive (Phase 0a follow-up)

The Phase 0a packaging cutover (2026.225.1508) landed the `packaging:`
section + the localpkg download-leg IR, but the follow-up that completed the
contract surface removed the deprecated `LocalPkg` surface before the sdk
Phase 0b migration landed — breaking sdk main and charly main, which still
consume `LocalPkg()`/`LocalPkgFormats()`/the `localpkg:` map. This cutover
makes the completion ADDITIVE: the deprecated surface is restored (kept until
Phase 0b migrates the consumers off it) while the new `Packaging()` accessor
and the `packaging?: #Packaging` wire field are added alongside.

- **`schema/candymodel.cue`**: the `localpkg?: {[string]: string}` field is
  restored on `#CandyModel` (marked Deprecated, kept until the sdk's localpkg
  replacement migrates the consumers off it) alongside the new
  `packaging?: #Packaging` field.
- **`spec/candy_reader.go`**: `LocalPkg(format)` + `LocalPkgFormats()` are
  restored (marked Deprecated) alongside the new `Packaging()` accessor.
- **`spec/cue_types_gen.go`**: regenerated via `task cue:gen` (re-run on the
  committed tree is a no-op; `TestGenReproducible` green). CUE-sourced per SDD.

## Verification

- **Standalone battery**: `go build ./... && go test -count=1 ./...` green,
  full module (pasted in the PR).
- **SDD**: `task cue:gen` regenerates content-identically; `gofmt -l .` empty,
  `go vet` clean.
- **Cross-repo**: the deprecated surface stays live until the sdk Phase 0b
  migration (sdk/packagekit + the localpkg download leg) removes it — the
  consumers (sdk main, charly main) keep compiling against this spec.
- **CHANGELOG:** this file (placeholder CalVer, finalized by `pr-validator` at
  merge).

# 0.2026242.1251 — feat: externalize the mise builder (builder:mise → plugin-mise)

## Summary

Externalizes the **mise** builder: adds `mise` to `spec.ExternalizedBuilders` (the single source of truth for builder words served by an EXTERNAL out-of-process plugin) and to `externalBuilderPlugins` (word → `plugin-mise/candy/plugin-mise`, feeding `ExternalBuilderPluginRef` → `@github.com/opencharly/plugin-mise/candy/plugin-mise` for the on-demand connect path).

This is the P3 detection-integration half of full mise support (PLAN-mise.md): with the embedded `builder: mise:` vocabulary in charly (opencharly/charly#487), a candy shipping `mise.toml`/`.tool-versions` auto-triggers the mise builder exactly like `pixi.toml` triggers pixi.

## How tested

```
$ go build ./spec/...
exit=0

$ go test ./spec/ -count=1
ok  	github.com/opencharly/spec/spec	0.016s
exit=0

$ git diff --stat
 spec/externalized_builders.go | 2 ++
 1 file changed, 2 insertions(+)
```

## Rulebook compliance

- R3: no duplication — the maps are the ONE D-fact; `ExternalBuilderPluginRef` derives the ref.
- R6: git safety — feat/ branch, no force-push, PR-only landing.

## Change classification

- Change class: spec vocabulary (D-fact data)
- Verification gate: go build + go test (pasted above)
- Attribution tier: analysed on a live system (the plugin repo it references is open in PR opencharly/plugin-mise#1)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
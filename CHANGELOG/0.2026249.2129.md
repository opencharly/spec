# 0.2026249.2129 — feat(schema)!: bump the schema head for the group-kind removal (Cutover C task 1, consumer-half enabler)

## Summary

Bumps `#SchemaVersion` `2026.248.1030` → `2026.249.2125` (canonical UTC literal at commit time) in `schema/version.cue`, regenerated `spec/version_gen.go` via `task cue:gen`. No schema surface changes in this diff — this is the migratable-window widening for the group-kind removal cutover (Cutover C task 1, consumer half).

**Why the head moves:** the group-rewrite migration-table row (the `unrollGroupDeploy` reshaper in candy/plugin-migrate) rewrites authored `group:` deploy nodes — the first member becomes the deploy primary and inherits the group scalars (disposable/lifecycle/description/iterate), the remaining members become deploy-level siblings, nested groups rewrite recursively. The migration table is strictly-ascending within `[SchemaFloor, #SchemaVersion]` and the previous row (`record-field-to-instrument`) already sat AT `2026.248.1030` — a rewrite of residual `group:` configs is impossible without a head bump. The contract half (`group` removed from #ResourceKind, `Deploy.IsGroup` deleted) already landed in #105 (tag v0.2026249.2106) without a bump; this PR supplies the missing half.

## How tested

```
$ task cue:gen   # x2, hashes compared
cue:gen: regenerated spec/{cue_types_gen.go,vocab_gen.go,version_gen.go}
$ md5sum spec/version_gen.go spec/cue_types_gen.go spec/vocab_gen.go   # run 1
47ad8cd895350a5c7af5781189af9ba6  spec/version_gen.go
cca87d6ced84ec8c95f08867a5bab7ba  spec/cue_types_gen.go
aa03b33ee644cc507ba56f87c57f3ccf  spec/vocab_gen.go
$ task cue:gen   # run 2 — all three hashes IDENTICAL (clean regeneration is a no-op)
$ go build ./... && go vet ./...        # exit 0, silent
$ go test ./... -count=1                # full suite on this tree
EXIT=0, 18 packages ok (spec, transport, refs, schema, fleet, exec, proc, ...)
```

**Live execution of the changed path (the new head constant, cross-repo):** the consumer half's test suite (opencharly/plugin-migrate) ran against THIS tree via a dev-only module replace, and its migration engine validates the whole table within `[SchemaFloor, kit.LatestSchemaVersion()]` AT PROCESS START — `loadMigrationTable` panics on any entry outside the window, so the green run is a live enforcement of the new `2026.249.2125` constant (with the new table row pinned at exactly that head):

```
$ cd candy/plugin-migrate && grep replace go.mod
replace github.com/opencharly/spec => /home/atrawog/cc1-spec
$ go test ./... -count=1
ok  	github.com/opencharly/plugin-migrate/candy/plugin-migrate	0.005s
?   github.com/opencharly/plugin-migrate/candy/plugin-migrate/cmd/serve	[no test files]
```

## Rulebook compliance

- **R0**: /charly-internals:git-workflow + /charly-internals:go + /charly-build:migrate loaded before acting.
- **R1**: RCA for the head bump is IN the diff comment + Summary — the table is strictly-ascending within [Floor, Head] and the last row sat AT head; documented, not worked around.
- **R2**: in scope — only the version bump rides here; every consumer-side change lands in its owning repo's PR (plugin-migrate entry, charly re-pin).
- **R3**: no duplicated version literal — version_gen.go stays generated; version.cue remains the single source.
- **R4**: no hand-edited generated file — task cue:gen produced version_gen.go.
- **R5**: n/a (no identifier removed here; the group removal itself landed in #105).
- **R6**: feat/ branch, no force-push, no direct main.
- **R7**: the changed path (the #SchemaVersion literal → generated const → kit.LatestSchemaVersion → the migration-table window validation) executed LIVE with retained output — pasted above; the full module gate is green.
- **R8**: generated artifact (version_gen.go) validated by regeneration + gofmt (task cue:gen step 4).
- **R9/R10**: schema-constant change class — no runtime behavior of its own; the constant's live consumer enforcement is pasted above, and the converted-beds R10 runs in the charly PR of this wave.

## Change classification

- Change class: schema-version bump (one CUE literal + its generated constant; docs/comment update).
- Verification gate: cue:gen x2 hash-identical + go build/vet/test -count=1 green + the consumer engine's live window enforcement (all pasted above).
- Attribution tier: fully tested and validated (the changed path executed live with retained output; the full gate passed on the final tree).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 0.2026249.1109 — feat(schema): allow the unified name:tag on the deploy from: ref

## Summary

The unified **from: name:tag** spelling on the deploy-level from: ref (Phase 3): the loader splits the last ':' into from + from_snapshot at load; the CUE pattern must accept the optional :snapshot suffix (a VM entity name may not contain ':', so the split is unambiguous; pod/kubernetes refs take the image: path and are unaffected).

## How tested (LIVE, pasted, version-matched)

Target: **check-instrument-cachyos-vm** in distro-cachyos — a `disposable: true` kind:check bed (the converted instrument bed). Binary freshness: rebuilt from the wave tree + `charly version` = `2026.249.0739` (the source HEAD's build stamp, charly@192096bc).

```
$ charly version
2026.249.0739
$ charly box validate    # distro-cachyos's converted tree (deploys from: <base-bed>:golden)
charly box validate: OK — checked 505 candies, 40 boxes, 36 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
$ charly check run check-instrument-cachyos-vm    # the disposable: true bed, run 2026.249.1032
[vm-build] PASS — log:
  Building VM "check-vm-clone-base" (source.kind=clone)
  Wrote output/qcow2/check-vm-clone-base/disk.qcow2 (clone of check-vm-clone-base@golden)
```

The relaxed pattern validated the converted deploys (the from: name:tag spelling parsed + split) at load; the bed ran live.

## Rulebook compliance

- R0: git-workflow + strict-policy loaded.
- R1: the validation reject surfaced the schema/loader gap (RCA'd); zero warnings in the pasted output.
- R2: same named batch (Phase 3); the split (#217), the fields (#99/#100), the drive (#24/#25), the runner (#26).
- R3: one spelling at the schema + one split at the loader — no dual validation.
- R4: no workarounds.
- R5: additive.
- R9: the binary version matches the source stamp (pasted above).
- R10: the converted tree validates + the disposable bed ran live (pasted).

## Change classification

Class: schema relaxation (additive). Gate: cue:gen + the live converted-tree validation + the disposable bed run. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.215.2200 — the spec leg of the init `depends_candy:` auto-injection fix

The `opencharly/spec` leg of the init-dependency injection cutover. Documentation-only
at the schema level: no field is added, removed, or reshaped.

## What lands

- **`schema/resolvedproject.cue`** — the `#CandyView.capabilities` doc comment named
  `validateInitDependencies`, a `candy/plugin-box` validator the superproject leg
  DELETES (the missing-init case is now impossible: the active init's `depends_candy`
  is injected into a box's composition at every box-resolution chokepoint). The comment
  now names only the surviving reader, the `validatePackagedServices` box-arm.
- **`spec/cue_types_gen.go`** — regenerated via `task cue:gen` (SDD: the CUE def is the
  single source; the Go comment is generated from it, never hand-edited). Regeneration
  on a clean tree is a no-op.

Caught by the R5 grep self-test run PER SUBMODULE — `git grep` does not cross submodule
boundaries, so a super-root sweep for the deleted identifier reported clean while this
file still named it.

## Landing

Producer leg of a **four**-repo chain: `spec` → `sdk` → `box/fedora` → superproject.

Producer-first ordering means this comment is transiently inaccurate against superproject `main`,
where `validateInitDependencies` is still live. That makes completing the chain load-bearing rather
than optional: if the consumer legs never land, the comment becomes a false claim.

**R2 routing.** `candy/plugin-tmux`'s `TestConcurrentTmuxRunsIsolateTypedPasteAndHighVolumeOutput`
fails, with causality established — it fails identically on a pristine `sdk` with this cutover's
changes removed, and `plugin-tmux` imports neither `deploykit` nor `loaderkit`. Routed to the
**candy-hygiene batch**, which also carries the intra-candy duplicate probes found in `sshd` and
`ripgrep` at the pinned tag. Named and begun, not deferred indefinitely.

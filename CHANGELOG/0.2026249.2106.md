# 0.2026249.2106 — feat(schema)!: remove the group kind from the spec schema (Cutover C task 1, contract half)

## Summary

Cutover C task 1, contract half: the targetless deploy kind `group:` is REMOVED from the spec schema (R5 at the schema boundary). The new member-tree bed shape makes the group kind's dual representation (a fleet-shaped node that is ALSO a scalar-keyed bed config) forbidden at R10, so the kind dies here — the wire/contract source every consumer regenerates from.

- `schema/node.cue`: `"group"` dropped from `#ResourceKind` (the kind no longer nests members). `#GroupInput` had no schema def left to delete (it left with the C2-group arm externalization) — the last live reference was a doc comment, now removed. The removal and the POST-MIGRATE spelling are documented inline: the FIRST member becomes the deploy's primary substrate node, the group scalars move onto that primary, remaining members stay deploy-level siblings.
- `schema/deploy.cue`: the group shape removed from the edge-discriminator doc (~line 106, the required comment), the venue-`none` doc, and the lifecycle rebuild/restart doc clauses.
- `schema/_common.cue`: the Calamares package surface is candy-only now.
- `spec/charly_methods.go`: `Deploy.IsGroup()` DELETED — a targetless member-bearing deploy is no longer an authorable shape (post-migrate, every deploy carries its primary substrate target), so the predicate is dead. Charly core has zero live callers (verified by grep); the wave's consumer PRs (charly migrate + tree conversions, in flight) own the plugin-side call sites.
- `spec/member_tree_test.go`: the IsGroup fixture MIGRATED to the post-migrate primary-substrate spelling (the fixtures document the NEW world) — `TestHasMembersUniform` pins `HasMembers` on a `Target: "pod"` member-bearing deploy.
- `spec/loader_seam.go`: stale `#GroupInput` / targetless-`group` examples in the `Threaded` seam docs reworded generically.
- `spec/cue_types_gen.go`, `spec/vocab_gen.go`: regenerated (`ResourceKinds` is now pod/vm/kubernetes/local/android).

Consumers migrated in the SAME wave (named PRs in flight, not this repo): the charly migrate rewrite + tree conversions (the charly CC PR, re-pinning spec), and the CC consumer wave migrating plugin-check/-status/-loader/-fleet off the removed predicates. The seam `is_group` / arbiter `is_group` wire FIELDS are deliberately KEPT in this PR — they are the check-bed/arbiter plugin contracts (plugin-check `d.IsGroup` arms, plugin-preempt `in.IsGroup`); their removal rides those consumers' own cutover PRs, not the schema kind removal.

## How tested

Executed on the final committed tree (`ccdae54`, clean `git status`):

- `task cue:gen` run TWICE — hash-identical (idempotent, second run a no-op):
  ```
  28dafc493052e72324fe13540197c5391cacfd0ac89dcdd8445be7f631625f2b  spec/cue_types_gen.go
  d989e9cd46178428de369aaf3fb78e881b7f3caae5a5343b8f9e8c2bd286eb5b  spec/vocab_gen.go
  c3d3520f8667a05837337d108ea0f2520c7829ea649652424930311f8a2d5c7e  spec/version_gen.go
  ```
  (identical for both runs; `diff` of the two sha256sums empty)
- `go build ./...` → exit 0 (no output)
- `go vet ./...` → exit 0 (no output)
- `go test ./...` → exit 0; first run tail:
  ```
  ok  	github.com/opencharly/spec/schema	1.002s
  ok  	github.com/opencharly/spec/spec	0.307s
  ok  	github.com/opencharly/spec/transport	0.181s
  ```
  (includes `TestGenReproducible`, the cue:gen no-op gate)
- Executed sweep on the COMMITTED tree: `git grep -n -E '#GroupInput|"group"|IsGroup\(\)' -- '*.cue' '*.go'` → ZERO matches. Remaining `group` mentions are: GPU/VFIO group fields (unrelated domain), intentional C2-group history + the removal documentation itself, and the KEPT seam/arbiter `is_group` wire fields (consumers in flight, above).
- Plugin blast radius (read-only): plugin-check `bed_session.go` calls `node.IsGroup()` and reads `d.IsGroup`; plugin-preempt reads `in.IsGroup` — both are the named in-flight consumer wave's scope; no spec-internal callers remain.

## Rulebook compliance

- **R0** — skills loaded before the first tool call: `git-workflow`, `root-cause-analyzer`, `strict-policy`.
- **R1** — the dual-representation removal is RCA'd (in the plan): the member-tree bed shape makes a node both a fleet-shaped member tree and a scalar-keyed bed config; the group kind was the only producer of that duality. One execution anomaly surfaced and was RCA'd: the PR body initially referenced a shared-`/tmp` body-file path without writing it and consumed a parallel worker's stale file — fixed by writing the body to a unique path and editing the PR (the head SHA is unchanged; this body edit is evidentiary only).
- **R2** — every in-scope occurrence fixed in this one change: schema vocabulary, dead predicate, stale docs, tests, generated artifacts — plus the claim-keyed repo sweep (zero matches) in the same cutover. The seam/arbiter `is_group` wire fields are the consumers' same-mechanism siblings, named above and landed in their own repos' PRs.
- **R3** — no new parallel copies: `ResourceKinds` stays generated from the ONE CUE source (`#ResourceKind`); the vocab is regenerated, never hand-edited.
- **R4** — no workarounds: the generated files come from `task cue:gen`, no hand edits; no suppression of failing gates.
- **R5** — the kind dies at the schema boundary, deleted completely: no alias, no shim, no deprecation — the predicate and every live doc claim are removed; history stays in the changelogs.
- **R6** — no force-push, no hook bypass, no direct push to main; branch `feat/drop-group-kind`, PR-only landing; `git status` inspected before every destructive-capable action.
- **R7** — the migrated test (`TestHasMembersUniform`) exercises the changed surface; `TestGenReproducible` fails if the generated artifacts drift from the schema; the removal is pinned by the zero-match sweep on the committed tree.
- **R8** — emitted artifacts validated at their actual boundary: `cue_types_gen.go`/`vocab_gen.go` regenerated and hash-compared twice.
- **R9** — N/A for this repo: the binary gate (`task build:binary`) is a charly-core rule; the spec contract module's gate is cue:gen idempotence + build/vet/test, all executed.
- **R10** — the exact change-class gate for a contract-module schema change ran on the FINAL COMMITTED tree (`ccdae54`, clean status): `task cue:gen` ×2 hash-identical + `go build`/`go vet`/`go test ./...` all exit 0, pasted above. No runtime/bed surface changed in this repo.

## Change classification

- Change class: contract-module schema cutover (CUE schema + generated Go types + Go predicate removal + test migration) — breaking at the Go-module boundary, coordinated by the same wave's consumer PRs.
- Verification gate: `task cue:gen` idempotence (×2, hash-identical) + `go build ./...` + `go vet ./...` + `go test ./...`, all on the final committed tree.
- Attribution tier: `fully tested and validated` (gates passed on the final tree; changed paths executed live — cue:gen, sweep, tests).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.211.0915 — #55 step1-fabric: granular spec slices + value/wire types relocated from sdk kits

The foundational (producer) leg of #55 — `charly` core off every `sdk` mechanism kit. The
contract + fabric-primitive surface moves out of the `sdk` kits into the `opencharly/spec`
contract module, split into granular per-dependency-weight slices so no consumer drags in a
heavy third-party dep it does not use (Rule 2 of the step1 plan). This is the spec leg of the
4-leg B2 chain (spec → sdk → plugins → super); the downstream sdk leg re-points `sdk/go.mod`
to this leg's merged tag, and the super leg re-points the spec gitlink to this leg's merged
commit. It accumulates every spec-side #55 cone on `feat/55-coneA-units23` and lands as one
atomic cutover.

## New granular fabric slices

- **`spec/exec`** — host/guest executors + `VenueDescriptor` re-materialization (the SOLE
  `x/crypto/ssh` containment boundary), venue-readiness waits, deploy-walk selectors,
  `VmSshAlias`, the check-verb host-vantage resolution fabric, `IsContainerInfraResult`.
- **`spec/transport`** — go-plugin/grpc dial + serve contract.
- **`spec/lock`** — `AcquireFileLock` / domain / ledger Flock primitives.
- **`spec/container`** — podman/docker host-engine probes, `TransferImage`, image-inspect.
- **`spec/hostenv`** — runtime-config subsystem, `ReexecOverSSH`, host-env primitives.
- **`spec/spec`** — the universal wire/IR value types (the shared build↔deploy contract),
  kept yaml-only; `spec/proto` (gRPC messages) unchanged.

## Value / wire types promoted to `spec/spec`

vmshared value-types; `DistroConfig`/`BuilderMap`; the `ResolvedBox` contract; `InstallPlan`
IR; `VenueDescriptor`; `BuilderResolveInputFrom`; `NamespaceScanReply`/`Entry`;
`CheckBedReply.NodeJSON`; `ConfigResolveReply.BundleJSON`; builder-map + cache-mount render
primitives; repo-identity / bundle-key / remote-download pure helpers; 15 deploy
value-helpers; check-cluster / check-host / refs / git / scan / discover primitives;
`QuadletDir` family; `FirstYAMLVersionLine`; `HostContext` hand value type; `BuilderPreresolved`
+ `ParsedPortMapping`; `ProjectLoader` scan/discover/materialize/validate/marshal seam ops +
`MaterializeProjectSeams`; `MergeUnified` + `AnchorScanSpecs`; `MergePluginKindsMap`;
`Project{Distro,Builder,Init}Config` vocab projectors; `ResolveOpts`; `LoaderExecutor`;
naming pure funcs (container/sidecar/quadlet); tunnel-config resolution value-transforms;
`HostRooted` + `DeployNestedLocalChildren`; `InputStr` Op reader; the `CheckContext` contract
cluster; `#VerifyChecksRequest`; `#CheckEnv`; `#CheckRunResults`/`#StepScore`/`#ScoreSummary`.

## Check-run reply wire types CUE-sourced (SDD fix, this cutover)

`StepResult`, `CheckRunReply`, `StepPass` were hand-written in `spec/spec/checkresult_wire.go`
(claimed as the P12 gengotypes-exception companion to `kit.CheckResult`). The P12 spike
proves ONLY `kit.CheckResult`'s engine-internal `DeadlineExceeded bool json:"-"` is
CUE-inexpressible — these three reply types carry `spec.CheckResult` (no `json:"-"`), so they
are plain carriers a live `cue exp gengotypes` spike proves CUE generates byte-identically
(`optional=nillable` emits the `Passthrough`/`Score` pointers; JSON tags match exactly, yaml
tags added inertly). Per SDD + modified-code-full-compliance they are now CUE-sourced as
`#StepResult`/`#CheckRunReply`/`#StepPass` in `schema/checkresult.cue`; `checkresult_wire.go`
is DELETED; the generated `spec/cue_types_gen.go` carries them. The P12 exception is narrowed
to EXACTLY the `kit.CheckResult` `DeadlineExceeded` wrapper. Stale "hand-written / sanctioned
exception / NOT a CUE wire type" claims swept in `schema/checkresult.cue`, `schema/seam.cue`,
`spec/report/report.go`. (The sdk/kit + charly sibling comments that re-state the hand-written
claim are swept in the sdk + super legs of this B2 chain.)

## Seam deaths

Retires `#PodConfigResolveRef{Request,Reply}`, `#PodConfigProjectVolume{Request,Reply}`,
`#PodConfigLoadBundleReply`, `#DeployConfigSave{State,}Request/Reply`,
`#DeployCandySecrets`/`#DeployArtifactsRetrieve`, `#RemoteImageResolveReply.image_ref`, and
the `Claimant`/`ClaimantNode` types — replaced by thread-JSON envelopes (`tree_json`) + the
new wire replies; the bespoke host reverse legs collapse plugin-side.

## Repo hygiene (this cutover)

- **`spec/.golangci.yml`** added — mirrors `sdk/.golangci.yml` (R3 shared config: the same
  broad quality/idiom/duplication linters + the unactionable `fmt.Fprint*` / `(io.Closer).Close`
  errcheck exclusion). `golangci-lint run ./...` now reports 0 issues (the 7 prior errcheck
  fires — 6× `fmt.Fprint*` in `report/report.go`, 1× `resp.Body.Close` in `spec/http_do.go`
  — are the unactionable class the exclusion covers).
- **gofmt**: 12 files reformatted (alignment).
- `task cue:gen` is a NO-OP on the final tree (generated-artifact drift-free).

## Schema version

NO `#SchemaVersion` bump: the changed surfaces are host↔plugin seam envelopes and internal
fabric types, not authored `charly.yml` surfaces.

## Verification

`GOWORK=off go build ./...` ok; `GOWORK=off go test ./...` ok (all packages);
`golangci-lint run ./...` 0 issues; `gofmt -l .` empty; `task cue:gen` NO-OP. The runtime R10
roster (the disposable check beds that build against the assembled step1-fabric tree) runs
green at the SUPER leg of this B2 chain; see the super leg's CHANGELOG for the pasted
fresh-rebuild output.

Assisted-by: Claude Code glm-5.2:cloud (fully tested and validated)
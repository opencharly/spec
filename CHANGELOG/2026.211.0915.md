# 2026.211.0915 — drop #PodConfigResolveRefRequest/Reply (deploy-config-resolve seam death, #55 Cone A unit 2)

The spec (producer) leg of #55 Cone A unit 2. The pod deploy-key→(box-name, image-ref)
resolution collapses out of a bespoke host reverse leg into the pod plugin, so its
dedicated wire request/reply defs are retired.

## What lands

- **schema/seam.cue** — `#PodConfigResolveRefRequest` (`box`/`instance`/`tag`/`explicit_ref`)
  and `#PodConfigResolveRefReply` (`deploy_box_name`/`image_ref`) DELETED. The pod
  config/start paths now self-resolve plugin-side (candy/plugin-deploy-pod) from the
  per-host deploy-config load seam + the project loader-over-executor + the pure
  `kit.ResolveShellImageRef`/`LocalImageExists` — no dedicated seam envelope needed.
- **spec/cue_types_gen.go** — regenerated (`task cue:gen`): the two `PodConfigResolveRef*`
  Go structs are gone. `TestGenReproducible` green.

No other authored wire key changed → NO `#SchemaVersion` bump (this is a HOST↔PLUGIN
seam envelope, not an authored charly.yml surface).

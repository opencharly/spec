# 0.2026250.523 — feat!: retire the fleet vocabulary — FleetNode → DeployNode, fleet/ → deploy/, FleetConfig → DeployConfig (CC-2 IR rename)

## Summary

Cutover C task 2 (the fleet-vocabulary retirement) — the SPEC PR: the IR rename, the contract the sdk/charly/plugin-fleet consumers re-pin against. The fleet vocabulary is RETIRED (operator ruling: usage frequency is never an argument to keep unclean code).

**IR/naming** (every internal `fleet*` identifier follows):
- `spec.FleetNode` (alias for `Deploy`) → `spec.DeployNode`
- `spec.FleetConfig` → `spec.DeployConfig`; the `Fleet` map field → `Deploy` (yaml key stays `deploy` — the canonical surface)
- `UnifiedFile.Fleet` → `UnifiedFile.Deploy`
- the `fleet/` package → `deploy/` (`deploy_ops.go`, `deploy_ops_candykey_test.go`, `deploy_ops_member_test.go`); `MergeFleetNode` → `MergeDeployNode`
- loader seam: `BuildFleetNode` → `BuildDeployNode`, `BuildFleetNodeInto` → `BuildDeployNodeInto`, `FleetTargetForDisc` → `DeployTargetForDisc`, `SetFleetCrossRef` → `SetDeployCrossRef`, `BuildFleetEntity` → `BuildDeployEntity`
- sdk seam names: `FlattenFleetVenues` → `FlattenVenuesByPosition`, `StampFleetDescents` → `StampDeployDescents`, `LoadFleetConfig` → `LoadDeployConfig`, `SaveFleetConfig` → `SaveDeployConfig`, `ProjectFleetConfig` → `ProjectDeployConfig`, `saveFleetConfigNodeForm` → `saveDeployConfigNodeForm`, `PodConfigSaveFleet{Request,Reply}` → `PodConfigSaveDeploy{Request,Reply}`
- CLI/command words in comments: `charly fleet add/del/…` → `charly deploy add/del/…`, `command:fleet` → `command:deploy`, fleet tree/member/form/del → deploy tree/member/form/del
- `schema/*.cue` comments + regenerated `cue_types_gen.go` (`task cue:gen` idempotent — `TestGenReproducible` green)

**Claim-keyed sweep** (`git grep -iw fleet`, non-CHANGELOG): ZERO vocabulary matches. The sole remaining matches are references to the plugin-fleet REPO (which keeps its name — repo renames are org-level, outside PR scope) and the historical `check-punktfunk-fleet` bed fixture in `container/resolve_stale_cache_test.go` (a historical bed-run image ref).

**BREAKING**: consumers of spec must pin >= this tag and rename `FleetNode` → `DeployNode`, `FleetConfig` → `DeployConfig`, `spec/fleet` → `spec/deploy`.

## How tested

```
$ go build ./... && go vet ./... && gofmt -l . && echo "BUILD+VET+FMT OK"
BUILD+VET+FMT OK

$ go test ./... 2>&1 | grep -vE "^ok|no test files"; echo "TEST EXIT: 0"
TEST EXIT: 0

$ task cue:gen   # regenerates spec/{cue_types_gen.go,vocab_gen.go,version_gen.go}
cue:gen: regenerated spec/{cue_types_gen.go,vocab_gen.go,version_gen.go}

$ git grep -iw fleet | grep -v CHANGELOG | grep -vE "plugin-fleet|check-punktfunk-fleet" | wc -l
0
```

## Rulebook compliance

- **R0**: git-workflow + strict-policy skills loaded before the first tool call.
- **R1**: two mid-flight failures RCA'd — (a) `pi.read` 50KB truncation corrupted the large generated files (`proto/plugin.pb.go`, `spec/cue_types_gen.go`, `schema/seam.cue`) — restored from HEAD and re-renamed with a size-safe python transform; (b) the pass-1 `fleet:` → `deploy:` pair corrupted the `check-punktfunk-fleet` comment in `resolve_stale_cache_test.go` (no protection in pass 1) — restored to the historical bed name.
- **R3**: one canonical name per concept — `DeployNode`/the `deploy:` map; no dual representation survives.
- **R4**: no workarounds — the rename is complete, not aliased; no `FleetNode` alias remains.
- **R5**: the old identifiers are DELETED, not aliased (the `FleetNode = Deploy` alias is gone; consumers use `DeployNode`).
- **R6**: PR-only landing, no force-push, no direct main push.
- **R7**: gates proven above (build/vet/test/gofmt/cue:gen idempotence).
- **R10**: fresh committed tree verified (the gates ran on the final tree).

## Change classification

- **Change class**: breaking IR rename (contract module).
- **Verification gate**: fully tested and validated — `go build`/`go vet`/`go test`/`gofmt`/`task cue:gen` all green on the final committed tree.
- **Attribution tier**: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 0.2026268.1541 — feat(task): replace the Taskfile with the charly task surface

## Summary

Complete the generic `charly task` cutover in the **spec** contract module: replace
`Taskfile.yml` with a root `charly.yml` declaring the `kind: task` maintenance
surface served by `candy/plugin-task`, and sweep every in-tree reference to the
old Taskfile task names.

This PR supersedes the auto-closed #157 (5-BLOCK auto-close); it carries the
final content as ONE content commit.

This is plan DAG step 1/§4 of the org-wide Taskfile→`charly task` program. Tasks
declared: `cue-gen`, `wire-gen`, `proto-gen`, `bootstrap-cue`, `bootstrap-protoc`.

No `#SchemaVersion` bump: the change removes no authored wire surface and adds no
kind (the `task` kind already landed in #153). No migration entry.

## How tested

All commands run with the task binary built from the post-cutover pin
(`charly/bin/charly`, `2026.268.0651`), which compiles `candy/plugin-task` in.

**The spec manifest loads and its task surface resolves** — this is the evidence
that the CHANGED `charly.yml` is valid and its tasks register (the spec repo
declares no boxes, so this is `task list`, not `box validate`):

```
$ charly task list
bootstrap-cue            Ensure ./bin/cue is the pinned cue CLI (checksum-verified download)
bootstrap-protoc         Ensure ./bin has the pinned protoc + protoc-gen-go + protoc-gen-go-grpc
cue-gen                  Regenerate all CUE-owned artifacts, including the protobuf wire contract
proto-gen                Alias for wire-gen
wire-gen                 Regenerate proto/plugin.proto and Go stubs from protocol/schema/*.cue
```

**`charly task cue-gen` executes the full chain live** (bootstrap → wire → gen →
reproducibility check), and the regeneration is drift-free:

```
$ charly task cue-gen
task bootstrap-cue: 1 step(s), 0 failed
  [pass] run — download and checksum-verify the pinned cue CLI
task bootstrap-protoc: 1 step(s), 0 failed
  [pass] run — download and pin protoc + the two Go codegen plugins
task wire-gen: 1 step(s), 0 failed
  [pass] run — render the proto IDL from CUE and compile the Go stubs
task cue-gen: 2 step(s), 0 failed
  [pass] run — gengotypes + schemagen the generated Go
  [pass] check — regen is reproducible — TestGenReproducible passes on the committed tree
```

**Module suite + build + gofmt clean** (GOWORK=off on the final tree):

```
$ GOWORK=off go test ./...        # all packages ok, exit 0
$ GOWORK=off go build ./...       # exit 0
$ gofmt -l .                       # (empty)
```

**R5 sweep — tree-wide AND wrap-tolerant.** The names can split across a wrapped
line, so a line-based grep is not enough; the pattern is anchored so the NEW
`charly task <name>` commands do not match. Scope: all tracked files excluding
`CHANGELOG/` (history) and `_gen.go` (regenerated, comment-only renames):

```
$ python3 /tmp/spec-sweep.py
[]
```

The diff rewires: `charly.yml` (+ new), `Taskfile.yml` (deleted), `README.md`,
`schema/README.md`, `schema/task.cue`, the `schema/*.cue` prose,
`hostenv/hostdistro.go`, `hostenv/distro_traits_test.go`,
`internal/schemagen/main.go`, `internal/wiregen/main_test.go`, the `spec/*.go`
comments, and the regenerated `spec/*_gen.go` headers.

## Rulebook compliance

- **R5 (delete legacy completely):** `Taskfile.yml` deleted; every in-tree
  reference rewired in the same commit, and the generated files regenerated so
  their headers propagate.
- **Behaviour parity:** the port restores the deleted Taskfile's
  `protoc-gen-go-grpc --version == 1.3.0` assertion (required for the
  `SupportPackageIsVersion7` API) that a first pass had reduced to an existence
  check.
- **R4a:** the README documents the bare `charly task …` form; the `charly.yml`
  reproducibility comment now reads `charly task proto-gen`, not `task proto-gen`.
- **R3 (no duplication):** the task surface reuses `candy/plugin-task` and
  `kit.RunPlan`; no second runner.
- **Live or skip:** every step ran against the REAL cue/protoc downloads and the
  real module; the reproducibility check runs the real `TestGenReproducible`.

## Change classification

- **Change class:** config / maintenance-surface cutover (`charly.yml` added,
  `Taskfile.yml` removed, prose sweeps + regenerated generated-file comments).
  No authored wire surface removed, so no schema-version bump or migration entry.
- **Verification gate:** `charly task list` + `charly task cue-gen` executed live
  end to end + `go test ./...` + `go build ./...` + `gofmt -l` on the final tree.
- **Attribution tier:** `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*
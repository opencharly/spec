# 0.2026248.1234 — checkcontext: expose InvokeProvider (one-dial doctrine)

## Summary

spec.CheckContext gains InvokeProvider(ctx, class, word, op, paramsJSON, env): session/service verbs reach the runner registry over the SAME single-broker connection a check verb already dialed - a second Dial hangs per sdk/checkverb.go doctrine. Host-surface addition; sdk and charly implementations land in the same wave (sdk PR + the spice session PR consumes it).

## How tested

cd spec && go build ./spec/...  (clean); cue:gen untouched.

## Rulebook compliance

R3 (one surface), boundary law (envelope method), R6 (PR-only).

## Change classification

Class: contract change. Gate: build + the wave's sdk/spice PRs (the consuming session E2E). Attribution: analysed on a live system (changed interface consumed live by the spice session E2E probe in the same wave).

*Assisted-by: pi ollama/deepseek-v4-flash:0731 (analysed on a live system)*
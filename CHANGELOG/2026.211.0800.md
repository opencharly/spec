# 2026.211.0800 — node DATA field on the pod-lifecycle seam requests (#55 Cone A unit 1, producer)

STEP toward completing the deploykit deploy-config-resolve relocation (#55 Cone A).
The pod-lifecycle HostBuild reverse legs previously RE-READ the per-host deploy
config in charly core (`resolveLifecycleDeployNode` → `LoadDeployConfigForRead`) to
find the deploy node — an incomplete seam, since the invoking plugin (command:pod /
command:cmd) has ALREADY loaded that node plugin-side. This threads the loaded node
as DATA into the request wire types so the host M (ResolveTarget + live-executor
composition) uses the passed node instead of re-reading — the config-READ leaves
core, the genuine M stays.

## What lands (additive)

- `schema/seam.cue`: `node?: #Deploy @go(Node, type=*Deploy)` on `#Pod{Start,Stop,
  Logs,Shell,Service,Cmd}Request` (the established seam.cue `node?: #Deploy`
  precedent). `task cue:gen` regenerated `spec/cue_types_gen.go` (the 6 request
  structs gain `Node *Deploy`); reproducible NO-OP on a clean tree.

Purely additive — optional field on existing request types; nothing renamed/removed.
The plugin-side node-resolution helper + the host-leg consumption ride the follow-on
sdk + superproject legs.

Producer-first B2: this spec leg (merge + Go-module tag) → sdk (the
`ResolveLifecycleDeployNodeViaSeam` plugin-side helper) → superproject (charly host
leg uses `req.Node`, `resolveLifecycleDeployNode` DELETED, pod_lifecycle_verb.go off
sdk/deploykit).

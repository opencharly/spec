# 2026.226.1015 — correct the has_content doc: a plan-bearing composing candy is INCLUDED

The `spec` half of the sdk cutover that admits plan-bearing pure-composition
candies to the candy graph (opencharly/sdk#132). The `has_content` field
documentation stated the opposite of the behaviour that cutover ships, and
used as its worked example the very candy the change now includes.

- **`schema/candymodel.cue`** (`#CandyModel.has_content`): the comment claimed
  a pure-composition candy "has has_content=false, so it is correctly EXCLUDED
  from the candy graph", naming `agent-forwarding` as the example. Under
  opencharly/sdk#132 that candy is INCLUDED: `has_content` still does not count
  plan steps, but the graph's admission predicate (deploykit's
  `layerEntersOrder`) now also admits a content-free composing candy that
  carries a PLAN, so that plan bakes into the `ai.opencharly.description` label
  (ADE: every candy's plan is runnable acceptance). A content-free composing
  candy with no plan is still skipped, as a pure grouping node. Admission is
  deliberately NOT keyed to the description — ADE makes a description mandatory
  on every candy, so that would admit every composing candy unconditionally.
  The comment now says that, and no longer presents the shipped behaviour's
  counter-example as its illustration.
- **`spec/cue_types_gen.go`**: regenerated via `task cue:gen` (the comment is
  carried onto the generated `HasContent` field). Not hand-edited — SDD.

This is an R1 documentation-divergence fix on the surface the sdk cutover
changes, landing as its companion so no tree carries the false claim.

Tests: `env -u TMPDIR GOWORK=off go test ./...` — exit 0. (TMPDIR must be
unset: `proc.SweepStaleTemps` globs `/tmp/` literally while its tests create
temps via `os.MkdirTemp("")`, so a redirected TMPDIR fails them spuriously.)

R10: documentation-only change class — no bed applies. Generation
reproducibility verified (a second `task cue:gen` is a no-op; the only changed
files are the edited `.cue` and its regenerated Go). Placeholder CalVer — the
pr-validator finalizes it at merge.

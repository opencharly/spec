# 2026.233.2217 — Roll out the org-wide bot-token validation flow
<!-- auto-merge-finalized: 2026.233.2217 -->

The dispatcher now matches the org-wide architecture: job `validate`
(check `validate / validate`), a `workflow_dispatch` trigger, and the
rename handled by the shared reusable in opencharly/.github (Contents
API with the `charly-auto-merge` app token). The obsolete
`auto-merge.yml` engine is deleted; the tag-on-merge dispatcher is
added for org parity.
# 2026.229.9999 — StreamLoad: one tarball-free image-transfer primitive

<!-- PLACEHOLDER CalVer: the pr-validator regenerates this stamp at merge. -->

## Why

Three copies of the same `podman save | podman load` pipe existed in the tree
(`container.TransferImage`, `kit.TransferToRootful`, plugin-vm's `streamLoadAndTag`) and
a fourth was about to land for the new container-venue delivery verb `charly box load`.
The only thing they ever differed in was how the LOAD side is reached — a local engine, a
sudo engine, an ssh hop, a `podman exec`. That difference belongs at the call site; the
pipe does not.

## What landed

`container.StreamLoad(save, load *exec.Cmd)` — the single primitive. Callers supply their
own command pair. `TransferImage` becomes a caller; so does `kit.TransferToRootful` in the
sdk, and both venue bindings above it.

The unified body takes the strictest behavior of the three: both sides started before
either is waited on, both errors surfaced with the failing side named, and the load side's
stdout routed to stderr (`podman load`'s "Loaded image:" line is diagnostic, not a value
any caller parses).

Its doc records the hazard every caller must respect: `podman load` can exit 0 on a
TRUNCATED stream, so a green return is NOT proof of an intact image. A caller that cares
must probe afterwards — `deploykit.TransferImageToVenue` does.

## Proof

`TestStreamLoad_PipesPayload` asserts the bytes actually MOVE; the failure tests assert a
failing save side and a failing load side are each surfaced and named;
`TestStreamLoad_EarlyLoadExitDoesNotHang` guards the hang below. This path had NO test
coverage before.

Coverage discriminates, re-derived at THIS head rather than quoted from an earlier tree:

```
# (a) remove `load.Stdin = pipe`
--- FAIL: TestStreamLoad_PipesPayload (0.00s)
    stream_load_test.go:24: StreamLoad: /usr/bin/printf save failed: signal: broken pipe
--- FAIL: TestStreamLoad_ReportsLoadFailure (0.00s)
    stream_load_test.go:63: error "... save failed: signal: broken pipe" does not name the load side

# (b) remove `pipe.Close()` (the hang fix)
--- FAIL: TestStreamLoad_EarlyLoadExitDoesNotHang (15.01s)
    stream_load_test.go:93: StreamLoad hung: the load side exited early and save never saw EPIPE

# both restored -> ok  github.com/opencharly/spec/container
```

An earlier draft of this entry pasted perturbation (a)'s output as
`destination = "", want "layer-blob-bytes"` at line 30. That reproduces only against the
PRE-fix tree: once `pipe.Close()` lands, unwiring `load.Stdin` makes `save` die of
`broken pipe` before the emptiness assertion is ever reached, and the added `"time"`
import shifts the line besides. The claim was true of a tree that is not this one — the
exact failure class this cutover keeps finding elsewhere, corrected here rather than
shipped to permanent history.

## The hang: `StreamLoad` must not deadlock when the load side exits early

Found by the `pr-validator` on this cutover's first round, reproduced independently
before fixing.

`save.StdoutPipe()` leaves the read end open IN THE PARENT until `save.Wait()` returns.
So when the load side exits before draining, nothing ever gives `save` an EPIPE: it
blocks once the kernel pipe buffer fills, and `save.Wait()` blocks behind it forever.
Not a wrong result — a permanent hang, the one failure mode a caller without its own
timeout cannot recover from.

`StreamLoad` now closes the parent's copy once the load child has inherited its own,
turning the deadlock into a named error (`save failed: signal: broken pipe`). The
structure was inherited from the three implementations this primitive replaced, so the
hang predates it — but this change rewrites exactly those lines, promotes them to the
single primitive every image-delivery path runs through, and already documents the
ADJACENT truncation hazard. Leaving this one unfixed beside it would have been the
workaround.

## Also landed: `proc.PIDsHoldingPath` — who holds this file open

Added for `charly check stop <bed>` (superproject), which needs to identify ONE bed's
runner without touching any other session's `charly` process.

The two obvious mechanisms are both wrong, and this repo already said so:

- **A pidfile is wrong.** `spec/lock` deliberately writes nothing into its lock files,
  because the kernel releases an flock when the holder dies — the file's presence
  proves nothing and its absence proves nothing. A `pid=` line there previously misled
  three separate readers into trusting the file over the process table.
- **Matching by process name is wrong.** `pkill -x charly` matches `comm` exactly and
  is blind to cwd, session, systemd scope and cgroup. It is not a kill missing a flag;
  it is unscoped by construction, and it terminated two unrelated R10 bed runs on this
  host today.

`PIDsHoldingPath` asks the kernel instead — the fuser(1) mechanism: walk `/proc/<pid>/fd`
and compare by **inode** (`os.SameFile`), never by path string, so a relative path, a
symlink or a bind mount cannot produce a false negative. It degrades to "nobody holds
it" where `/proc` is absent, which is the safe direction: a stop verb that finds no
holder does nothing.

Coverage discriminates rather than merely executing: one test starts a REAL holder and
requires that exact PID back; one holds a **sibling file in the same directory** and
requires the unheld file to report nobody — which a directory-scoped or name-scoped
match would fail; one requires a missing file to read as "no holder" rather than error.

# 2026.223.0000 — add Tag to DeployTargetRebuildOpts for pinned-image rebuilds

`charly update --tag <calver>` must re-deploy a **specific** image tag instead
of always resolving the newest local CalVer. `DeployTargetRebuildOpts` gains a
`tag` field, threaded from the update dispatch
(`update_deploy_dispatch.go` in the consumer) through the rebuild. Empty =
existing behavior (newest local CalVer resolved via the
`ai.opencharly.version` OCI label) — fully backward compatible.

- **`schema/seam.cue`**: `#DeployTargetRebuildOpts` gains
  `tag?: string @go(Tag)` with a doc comment.
- **`spec/cue_types_gen.go`**: regenerated via `task cue:gen` (a re-run on the
  committed tree is a no-op — tree clean before and after; `TestGenReproducible`
  green). CUE-sourced per SDD — no hand-written wire struct.

## Verification

- **Standalone battery** (`GOWORK=off go build ./... && go test -count=1
  ./...`): green, full module (pasted in the PR).
- **SDD**: `task cue:gen` regenerates content-identically; `gofmt -l .` empty,
  `go vet` clean, `golangci-lint run ./...` 0 issues.
- **Exercised live**: the disposable `check-agentteams-core-pod` R10
  fresh-rebuild leg pins the per-run bed tag (`charly update --tag
  <bed>-<runCalver>`), the rebuild re-deploys to that pinned image, and
  `check-live-rebuild` PASSes on it — proving the tag threading end to end (no
  stale-image reuse). Gate run 7 (`2026.223.1239`): 13 steps, zero warnings,
  pasted in the PR.
- **CHANGELOG:** this file (placeholder CalVer, finalized by `pr-validator` at
  merge).

---

*Assisted-by: Claude Code deepseek-v4-flash:cloud (fully tested and validated)*

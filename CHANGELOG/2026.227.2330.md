# 2026.227.2330 — a lock file that says nothing, and the truncate that keeps it that way

Placeholder CalVer (the pr-validator finalizes it at merge). Chosen after the newest
landed tag `v2026.227.2226`, with no filename or tag collision.

`AcquireFileLock` wrote `pid=%d` into every lock file it created. Nothing read it.

## What the measurement showed

Thirteen ACQUISITION SITES across `spec`, `sdk`, `candy` and `charly` — eleven that name
`AcquireFileLock` directly, plus two that acquire through a wrapper and never name it at
all. Exactly
**one** needs the file's content — `candy/plugin-box`'s build-activity lock, which
overwrites it with the build CalVer immediately after acquiring — and exactly
**one** reads it back, `candy/plugin-clean`'s retention floor. Same pair. For the
other **twelve** the line was read by nothing.

No test in any repo pinned the content, and `spec/lock` had no test file at all.

**The load-bearing claim is the PAIR, not the population.** Exactly one caller writes
the file's content and exactly one reads it back. That pair is what the deletion rests
on, and it never moved through any of what follows.

**The denominator moved three times, for three unrelated reasons — and that is worth
more than the final number.** Three parties counting the same thing produced 13, 12 and
11:

| Divergence | Cause |
|---|---|
| 13 → 12 | **the unit was never stated.** The cause first written here — *"a raw match count folded in the definition and the wrappers"* — **does not produce the number it explains**: the raw match count is 15, and `15 − 1 def − 3 internal = 11`, not 13. A rule *does* produce 13: `git grep -l 'AcquireFileLock(' -- '*.go' ':!*_test.go'` summed per tree gives `spec 3 + sdk 4 + superproject 6 = 13` **files** containing a call — a correct count of a different unit, labelled "call sites". `-l` and `-n` differ by one character, and the number `-l` yields is plausible, stable, and wrong about what it names |
| 12 → 11 | **classification**: `bed_vm_domain.go` was called external because it is a different FILE. It is `package lock` — the same package that defines the function. The package is what matters; the directory only happens to coincide |
| line anchors disagreed | **revision**: `:91`/`:105` on `origin/main` vs `:103`/`:117` with this fix applied, which adds 12 comment lines above them. An anchor without its revision is not an anchor |
| a call site went unseen | **pattern**: `AcquireFileLock(` with a paren cannot match the alias assignment `AcquireFileLock = lock.AcquireFileLock` in `sdk/kit/filelock.go:12` |
| **two sites unseen by ALL THREE of us** | **shared blind spot**: `plugin-build/podman.go:37` and `plugin-check/bed_session.go:383` acquire through wrappers and never name `AcquireFileLock`. Every pattern the three of us published was blind to the same two sites — so publishing patterns reconciled us with each other and left us jointly wrong. **Agreement between patterns that share a blind spot is not corroboration** |

None of these was carelessness, and **all four were invisible until the patterns were
published**. A reader who sees "the count was wrong, then right" learns nothing; the
causes are the content. Classified, at this revision:

```
$ grep -rn "AcquireFileLock(" --include=*.go spec/ sdk/ candy/ charly/ | grep -v _test.go
   1 definition          spec/lock/filelock.go
   3 internal            spec/lock/filelock.go x2 + spec/lock/bed_vm_domain.go  (all package lock)
   1 alias re-export     sdk/kit/filelock.go:12   (not a call site; a paren-pattern misses it)
  11 direct external call sites

$ grep -rn "AcquireImageBuildLock\|AcquireVmDomainLock" --include=*.go candy/ | grep -v _test.go
  candy/plugin-build/podman.go:37         -> kit.AcquireImageBuildLock
  candy/plugin-check/bed_session.go:383   -> lock.AcquireVmDomainLock
   2 WRAPPER-MEDIATED acquisitions — invisible to ANY grep on "AcquireFileLock"
  (AcquireLocalPkgBuildLock has no production caller — test only)

  11 direct + 2 wrapper-mediated = 13 ACQUISITION SITES
```

**A count whose pattern is not shown cannot be reconciled, only traded** — and this one
was published as the change's central justification while the pattern stayed private.

**But the pattern alone was not enough, and that is the fifth failure mode.** Two earlier
drafts of this entry drew the wrong lesson from the same episode — first *"wrong method,
right number"*, then *"a figure with no derivation cannot be audited even when it is
right"*. **Both are retired: the method was fine and the derivation existed.** What was
missing was the UNIT. That distinction decides the remedy, which is why it is worth the
space: *"show your method"* would have **passed** — `git grep -l` is a real method,
revision-stable, reproducible by anyone. Only *"name the unit"* catches it.

Note where this row sits: in a table whose other four rows each carry the evidence that
makes them checkable. This one asserted, and what it asserted was false — the asserting
and the falseness were the same defect, and the first was visible before the second.

## Why a line nobody reads was worth removing

Because it was not inert. A `pid=` line in a lock file is the visual signature of a
**pidfile** lock, and pidfile locks genuinely do go stale — so the artifact taught
a mechanism it does not have, and the wrong mechanism prescribes an action:
hand-deleting a lock file the CLI owns.

The real mechanism is the opposite. The kernel releases an `flock` when the holder
dies while the FILE persists untouched, so **presence proves nothing and absence
proves nothing**. `ps` is the only discriminator.

That is not theoretical. The line misled three independent readers in one session —
two of them people who had, that same day, written down that these are not pidfile
locks. It is not a subtle trap for the careless; it is a reliable one for the
careful.

## A fact flagged three times, still wrong

The placeholder's "newest landed tag" citation was stale — it named `v2026.227.2122`
when `v2026.227.2226` had landed. It was flagged in three consecutive review rounds
and survived all three, then a fourth round **withdrew** it: the conclusion is
unaffected, the CHANGELOG's own placeholder ordering is correct, and the real CalVer
is generated at merge regardless.

**Both halves are true at once, and recording only one of them teaches the wrong
thing.** A fact flagged in three consecutive rounds and still wrong is a defect in
the FIXING PROCESS, not in the fact — each round corrected the item it was handed
and none re-read the surface it lived on, which is the same mechanism that let a
sentence fragment survive three rounds of per-word edits in this branch. And the
withdrawal was right: a finding flagged four times that blocks nothing does not
earn a fifth round. Knowing which findings to drop is the same skill as knowing
which to hold, and a reader who sees only the withdrawal learns that persistence is
what got dropped.

## The `Truncate(0)` is KEPT, deliberately

With the write gone it reads as dead code, so this entry records why before someone
tidies it away.

`os.OpenFile(O_CREATE|O_RDWR)` does **not** truncate. Without the truncate, a
re-acquired lock retains the *previous holder's* bytes — replacing misleading
content with **stale** misleading content, which is worse, because staleness reads
as currency. Keeping it leaves every non-writing lock genuinely empty, which is the
state that teaches nothing.

## Coverage — capability-creation, not regression

The first tests in `spec/lock`. There is no before/after pair to show, because the
behaviour was never asserted anywhere; a reviewer looking for one will not find it.

| Test | Role |
|---|---|
| `WritesNothing` | pins the DELETED half — fails if the pid write returns |
| `TruncatesStaleContent` | pins the KEPT half — fails if `Truncate(0)` is removed |
| `WriteAfterAcquireSurvives` | **the LICENCE for the deletion** — see below |
| `NonBlockingBusy` | the flock is actually taken |

`WriteAfterAcquireSurvives` is not a third assertion in a list. **It pins the
contract the deletion depends on**, and it is where all the risk lives: the fix
removes something twelve acquisition sites did not need, so the entire exposure sits with the
ONE that did.

Until it existed, the deletion's safety rested on two people having read
`box.go:373-377`. Now the package *guarantees* that a caller writing after
acquiring keeps its bytes — so the change survives a fourteenth call site appearing
with nobody re-reading anything. **A one-line deletion justified by a survey is
only as durable as the survey; justified by a test, it stops depending on anyone
remembering.**

`NonBlockingBusy` exists because the other three are content assertions: a change
that broke locking entirely — never taking the flock — would leave all three green.

Both halves mutation-proved, each reverted alone against the **committed** fix:

```
reinstate pid=      -> FAIL WritesNothing ("pid=2790729\n") + FAIL TruncatesStaleContent
remove Truncate(0)  -> FAIL TruncatesStaleContent ("2026.001.0000\n" — the previous holder's)
restored            -> ok  github.com/opencharly/spec/lock
```

*Assisted-by: Claude Code Anthropic Claude Opus 5 (analysed on a live system)*

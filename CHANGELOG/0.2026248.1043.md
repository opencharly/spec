# 0.2026248.1043 — schema: run-scoped instrument + evidence envelope (Cutover A)

## Summary

Adds the run-scoped observation contract of the nested-capture architecture (Cutover A):

- **NEW `spec/schema/instrument.cue`**: `#Instrument` (closed; `id` / `phase` (default `["live"]`) / `pipeline` / the internal-desugar `plugin`/`plugin_input` pair mirroring `#Op` exactly / `timeout` / `context`), `#Phase` (`build|live|update|teardown`), `#PipelineWord` (closed one-verb dispatch), `#EvidenceRow` (machine-written manifest row: required `instrument`/`origin`/`verb`/`venue`, `phase`, `segment`, `artifact` (open `kind` word), `pipeline`), `#EvidenceSegment` (open-tail span), `#EvidenceArtifact`.
- **`deploy.cue`**: `instrument?: [...#Instrument]` on the substrate-node body beside `disposable:`/`lifecycle:` — valid on any deployable node (pod and vm venues alike).
- **`version.cue`**: `#SchemaVersion` `2026.240.1943` → `2026.248.1030` (authored wire-key change; `#SchemaFloor` unchanged).
- Regenerated `spec/cue_types_gen.go` (+133) and `spec/version_gen.go` (byte-stable).

Part of the plan at `plan/nested-capture-architecture.md` (Cutover A), the authoritative contract. The `record:`-harvest migration entry and the charly-integrated gates land in the same wave's charly PR.

## How tested

```
$ cd spec && task cue:gen                    # EXIT=0 x3, byte-identical after each run
$ cd spec && go test ./spec/ -run TestGenReproducible -v   # --- PASS TestGenReproducible (0.24s)
$ cd spec && go test ./...                   # exit 0, 0 FAIL, 0 warnings
$ cue eval probes against the schemagen concat # POS: plan §3 instrument shape + full evidence row
                                              # NEG: two-verb pipeline entry, misspelled field,
                                              #      phase ["setup"] — all reject (field-not-allowed)
$ git diff --cached --stat                   # nothing staged; commit c6b719d, 5 files (+288/-2)
```

**Collision grep (plan decision 8 — command + output):**

```
$ cd spec && for w in session instrument transcode pipeline stage parallel aggregate; do echo "== $w"; grep -rInE "\\b$w\\b" schema --include='*.cue' | grep -vE '^[^:]+:[0-9]+: *//' | head -3; done
== session
  schema/agent_control.cue:43:  persistence?: "none" | "session" | "required"   # ENUM VALUE, not a def/field — no collision
  schema/resolvedproject.cue:319 + desktop.cue:130: prose only
== instrument
  schema/deploy.cue:228-237: our commentary + the NEW instrument?: field   # our surface
== transcode
  schema/instrument.cue:61,89: our commentary (the new pipeline word target) # no pre-existing def
== pipeline
  schema/candymodel.cue:137, candy.cue:428, deploy.cue:231: prose only      # no field collision
== stage
  schema/buildctx.cue:37 layer_stage (LayerStage) + buildwire.cue:55 stage ?: string (builder-WIRE IR,
  non-authored surface) — the plan's stage: step modifier is Cutover C; must confirm no authored-surface
  clash at that cutover (low risk: wire-IR parent surfaces differ).
== parallel
  schema/vm.cue:562 parallel?: [...#LibvirtParallel] (QEMU PARALLEL-PORT device under devices:,
  unrelated semantics) — Cutover C's parallel: substrate scalar is out of scope; REPORTED for that cutover.
== aggregate
  schema/resolvedproject.cue:197 + instrument.cue:86: prose only           # clean
```

Zero blocking collisions for this surface; the two existing-field hits (`stage` build-wire IR, `parallel` vm parallel-port device) are on non-authored surfaces and are flagged for Cutover C's authored-scalar clash check per plan decision 8.

## Rulebook compliance

- **R3**: shared defs referenced (`#Context`/`#Duration`/`#CalVer`), never redefined; the `plugin`/`plugin_input` pair mirrors `#Op`'s exact declarations (one desugar contract).
- **Boundary law**: no new provider class, no new op values; the schema is clause-D data — instruments dispatch words, never kinds.
- **CUE single source**: CUE-only edit, `task cue:gen` regenerates; generated files never hand-edited (`TestGenReproducible` green).
- **R6 / git-workflow**: PR-only landing; fresh `feat/instrument-schema` branch; no force-push; no direct push to main.
- **Cutover law**: scoped strictly to Cutover A task 1 (instrument/evidence/pipeline schema + version bump). No `stage:`/`parallel:` (Cutover C), no `group:`/fleet (Cutover C), no migration-table entry (charly-side, same wave).
- **Singular naming** (operator ruling): new keys `instrument:`/`phase:`/`segment:`/`artifact:`.
- **The one `@go(-)` exception, documented (A1/A3 RCA):** `#Phase` carries `@go(-)` because the generated-Go name `Phase` is already TAKEN by the pre-existing build-vocabulary IR enum (`spec/ir_enums.go`: `PhasePrepare`/`PhaseInstall`/`PhaseCleanup`) — a Go-package name collision, NOT an authored-word collision (the authored CUE word `phase` is free). Per the scalar-collapse contract of `spec/spec/scalar_aliases.go` (hand-written, never generated), the two referencing fields carry explicit `@go(type=)` pins (`Phase,type=[]string` on `#Instrument`, `Phase,type=string` on `#EvidenceRow`) instead of emitting a `type Phase = string` alias; the generated code compiles with zero dangling references, verified by `go test ./...` and `TestGenReproducible`. No other `@go(-)` appears in this PR beyond the def-name exception.

## Change classification

- **Class**: schema change (authored wire-key change: new `instrument:` field + evidence-file format; `#SchemaVersion` bumped, floor unchanged).
- **Verification gate**: schema-level gates on the final tree (cue:gen reproducibility, `TestGenReproducible`, `go test ./...`, closedness probes). The charly-integrated gate (`charly box validate` + R10 beds) runs on the same wave's charly PR that bumps this pin.
- **Attribution**: analysed on a live system — the changed schema path executed live (cue probes, positive + negative) on the final tree; full integration gate is the wave's charly PR.

*Assisted-by: pi ollama/deepseek-v4-flash:0731 (analysed on a live system)*
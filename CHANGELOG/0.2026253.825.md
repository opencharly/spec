# 0.2026253.825 — fix(exec): WaitForSSH aborts immediately on a permanent ssh failure (the check-live hang regression)

## Summary

The check-live hang regression, fixed at the root: `charly check live` against a VM whose managed ssh-config alias is missing hung for the full readiness cap (~10 min) instead of failing fast. The WaitForSSH probe returned only a ready-bool and never an error, so "Could not resolve hostname" was treated as transient and polled to the cap.

The fix: the probe captures stderr and returns the poll contract's ErrPollFatal on a PERMANENT failure (unresolvable alias, stale known_hosts entry) — the poll aborts immediately. "Permission denied" is deliberately NOT permanent: a first-boot guest's authorized_keys is seeded by cloud-init AFTER sshd comes up, so auth refusal is transient during the boot window.

## How tested (pasted)

```
$ go test -count=1 -run TestIsPermanentSSHFailure -v .
=== RUN   TestIsPermanentSSHFailure
--- PASS: TestIsPermanentSSHFailure (0.00s)

$ go test -count=1 -run TestWaitForSSH_MissingAliasFailsFast -v .
=== RUN   TestWaitForSSH_MissingAliasFailsFast
--- PASS: TestWaitForSSH_MissingAliasFailsFast (3.77s)

$ go test -count=1 ./...
ok  	github.com/opencharly/spec/exec	8.162s
```

The integration test runs the REAL ssh probe against a missing alias with a 30s cap-only poll: the fix fails in ~3.8s with the ErrPollFatal-wrapped error naming the unresolvable hostname — the cap is never burned.

## Rulebook compliance

- R0: the root-cause-analyzer skill loaded; the hang traced to the probe's missing error path.
- R1: the hang RCA'd (the probe never returns an error → the poll treats a permanent failure as transient).
- R3: the fix uses the EXISTING poll contract (ErrPollFatal) — no new mechanism.
- R4: no workarounds — the probe now reports the permanent failure honestly.
- R6: no force-push, no hook bypass, PR-only landing.
- R10: the unit + integration tests pasted.

## Change classification

- Change class: fix (the check-live hang regression)
- Verification gate: the unit + integration tests (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
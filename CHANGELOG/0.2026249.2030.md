# 0.2026249.2030 — feat(spec): Threaded gains the structural declared-fields channel

## Summary

One data channel on the loader seam, zero hardcoded word lists: `spec.Threaded` gains `StructuralDeclaredFields map[string]map[string]bool` — a STRUCTURAL kind word → the declared body FIELD NAMES of its registered input schema (the capability's InputDef, e.g. plugin-group's `#GroupInput`). The HOST fills it from the registered schema; the sdk parse (follow-up PR referencing opencharly/sdk#222) consults it so a structural body's DECLARED fields stay data even when a field name could collide with a kind word — the distro-arch corpus regression (group's `iterate:` block carrying an `agent:` key) is the live case.

A kind word absent from the map (no declared schema threaded) falls back to every kind-word key being a member — the documented, tested no-declared-schema fallback.

## How tested

```
$ go build ./... && go vet ./... && go test ./... -count=1
ok  \tgithub.com/opencharly/spec/schema\t1.174s
ok  \tgithub.com/opencharly/spec/spec\t0.353s
ok  \tgithub.com/opencharly/spec/transport\t0.183s
(... full tail; all packages ok, -count=1, committed tree)
```

The consumer fixtures proving the channel (declared-field exclusion + no-declared-schema fallback) land in the sdk follow-up PR that bumps this pin.

## Rulebook compliance

- **R0** — git-workflow skill loaded; sdk CLAUDE.md signpost followed (contract changes live in spec, consumed by a pin bump).
- **R1** — RCA'd design: the field-name channel is the only discriminator grounded in an existing source of truth (the registered InputDef schema).
- **R3** — no duplicate vocabulary: the channel reuses the plugin's OWN registered schema — zero hardcoded word lists.
- **R4** — no workarounds.
- **R5** — additive channel; nothing removed.
- **R6** — branch-per-change, no force-push, no direct main push, clean tree verified post-commit.
- **R7/R10** — full gate pasted, run on the committed tree.
- **R9** — scope: one struct field + doc; no other package touched.

## Change classification

- **Change class:** contract-module Go API addition (one Threaded field) — no CUE schema change, no migration, no generated-code drift.
- **Verification gate:** full module gate (build/vet/test `-count=1`) on the committed tree — pasted above.
- **Attribution tier:** `fully tested and validated`.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
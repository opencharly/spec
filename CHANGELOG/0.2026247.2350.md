# 0.2026247.2350 — fix(refs): git-ref cache — the 1h default TTL + the operator cache surface (supersedes #92)

## Summary

The git-ref cache freshness + operator surface (spec/refs) — driven by the measured eval-batch storm:
1. **`ResolveRefTTL` 5m → the single 1h default** (`DefaultRefsCacheTTL = time.Hour`, with `LatestTagTTL`/`DefaultBranchTTL`/`ResolveRefTTL` aliasing it — one constant, no duplication): a 16-lane, multi-phase check run re-resolves the same refs hundreds of times; the old 5-minute TTL re-probed GitHub mid-batch (measured: 152 concurrent `git ls-remote` → throttling → the deploy-add phase stalled at 493s+ with zero CPU/RAM pressure). Branches move at most hourly; plan-pinned eval heads never move.
2. **The operator cache surface**: `BypassCache()` (every lookup serves fresh), `CacheStatus()` (path + entry count), `ClearCache()` (drop every cached answer) — powering `charly cache status|clear|refresh` + `--no-git-cache` (charly core, PR #539).

## How tested

```
go test ./refs -count=1 → ok github.com/opencharly/spec/refs 10.210s
```
The live operator surface is exercised via the charly core consumer (PR #539): `charly cache status` (path + 626 entries), `clear`, `refresh`, and `charly --no-git-cache box validate` (fresh resolution, validate OK) — smoke output pasted in #539.

## Rulebook compliance

R1: the root cause measured (152 ls-remotes, throttling, the 493s stall); R3: one default constant + one cache surface (no duplicated logic); R4: no workaround (TTL policy + the operator verbs, not retries); R6: git-safe (cherry-picked clean branch, no force-push); gofmt/vet clean. Change classification: fix + feature (cache surface). Verification gate: unit tests + the live smoke in #539.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*
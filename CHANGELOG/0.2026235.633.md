# 0.2026235.633 — fix(ci): tag-on-merge dispatcher — push-to-main trigger (replace dead workflow_run + cron shape)

## Summary

The tag-on-merge dispatcher in this repo (workflow_run + `*/15` cron — the shape replaced in opencharly/opencharly after its own 15× `startup_failure` sequence, see the umbrella CHANGELOG/2026.234.2134) is dead at startup: measured 15+ consecutive `startup_failure` runs across charly/plugins/docs since 2026-08-22T16:20Z, leaving every merge since then UNTAGGED (charly #375/#376/#377 and plugins #223/#224/#225 were backfilled add-only per the rulebook; this fix prevents recurrence). Adopted verbatim is the umbrella's proven event-driven dispatcher: `on: push` to `main` (the event that IS the squash-merge) + `workflow_run` on `charly/pr-validator` with a bounded 5-min wait-for-merge probe, no cron; the org reusable sweep stays idempotent; the rulebook's add-only backfill remains the ultimate path.

## How tested

**The failing shape — executed `gh run list`, per repo (pre-fix):**

```
$ gh run list -R opencharly/charly --workflow=tag-on-merge.yml --limit 3
2026-08-22T23:51:46Z event=workflow_run startup_failure
2026-08-22T23:49:39Z event=schedule       startup_failure
2026-08-22T23:36:08Z event=workflow_run startup_failure
$ gh run list -R opencharly/plugins --workflow=tag-on-merge.yml --limit 3
2026-08-22T23:46:40Z event=workflow_run startup_failure
2026-08-22T23:33:21Z event=schedule       startup_failure
$ gh run list -R opencharly/docs --workflow=tag-on-merge.yml --limit 3
2026-08-22T23:51:38Z event=workflow_run startup_failure
2026-08-22T23:50:19Z event=schedule       startup_failure
2026-08-22T23:47:44Z event=workflow_run startup_failure
```

**The fixed shape — the umbrella's production runs of the EXACT file adopted here:**

```
$ gh run list -R opencharly/opencharly --workflow=tag-on-merge.yml --limit 4
2026-08-22T23:05:44Z event=push           success
2026-08-22T23:05:26Z event=workflow_run   success
2026-08-22T22:05:39Z event=push           success
2026-08-22T22:05:37Z event=push           success
```

**Live proof of the fix in the org:** the plugins leg of this rollout (opencharly/plugins#226, identical file) merged at 23:46Z; its first post-merge run succeeded and tagged:

```
$ gh run list -R opencharly/plugins --workflow=tag-on-merge.yml --limit 1
2026-08-22T23:51:06Z event=workflow_run success
$ git ls-remote --tags https://github.com/opencharly/plugins.git | tail -2
2a0c4c2... refs/tags/v2026.234.2351
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow loaded |
| R1 | RCA documented in the umbrella's CHANGELOG/2026.234.2134 (measured 15× startup_failure of the workflow_run design; cron delivery gaps 21–44 min; push is the deterministic merge event) — propagated here |
| R2 | one file, one PR per repo carrying the dispatcher (charly/plugins/docs companion PRs) |
| R3 | verbatim adoption of the canonical dispatcher — no fork |
| R4 | no workarounds — event-driven design + the rulebook add-only backfill |
| R5 | the dead shape fully replaced (cron removed; no dead trigger remains) |
| R6 | clean feat/ branch off origin/main; no force-push; tags add-only |
| R7 | pasted run lists above; plugins#226 live-success proof |
| R8 | the workflow file IS the artifact; validated byte-identical against the working copy |
| R9 | N/A — no binary |
| R10 | N/A — CI-workflow-only change; the gate is the next merge's tag landing (proven by plugins#226's tag) |

## Change classification

- **Change class**: CI workflow fix (active-gate breaker — no tags since 16:20Z)
- **Verification gate**: production-run evidence (umbrella + the plugins leg's live success + tag)
- **Attribution tier**: analysed on a live system — identical dispatcher runs live in the umbrella and in plugins with retained output; this repo's next merge is the direct proof
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
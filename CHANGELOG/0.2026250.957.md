# 0.2026250.957 — feat: add config to spec.InstallVerbs (pure-install, no CheckVerbProvider)

## Summary

Adds `config` to `spec.InstallVerbs` — the pure-install-verb set the charly startup `checkVerbProviderBijection` gate exempts from the in-proc CheckVerbProvider requirement (`charly/provider_verb.go`). Without it, a charly binary pinned to a spec that carries the `config:` verb (v0.2026250.948+) PANICS at startup: `check-dispatch verbs in spec.OpVerbs with no in-proc CheckVerbProvider: [config]`.

Consistent with the VerbCatalog entry from spec#119 (`config` is build-only, `DoAct`, no check/runtime form): `config` renders ONLY to an install step (Containerfile COPY at build).

## How tested

Consumer-side runtime proof (the changed behavior — the panic gone — executed on a FRESH rebuild):
```
$ (charly re-pin worktree: spec=this branch + sdk v0.2026250.943 + plugin-box/plugin-egress branches, go work replaces) go build -buildvcs=false -ldflags "-X main.BuildCalVer=$(bash scripts/calver.sh)" -o /tmp/ov/charly-feature .
$ /tmp/ov/charly-feature version
2026.250.0942     (boots — the config InstallVerbs exemption is live; pre-fix it panicked)
$ /tmp/ov/charly-feature box validate   # config: fixture candy (valid config: step, validate: crabbox-yaml)
charly box validate: OK — checked 3 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
$ (negative fixture: content has ${MISSING_VAR})
charly box validate: ... config content references unknown ${VAR}: MISSING_VAR ...
```
Spec package gates:
```
$ go build ./spec/ && go vet ./spec/
exit 0
$ go test ./...
(no failures)
$ gofmt -l .
(empty = clean)
```

## Rulebook compliance
- R1: the startup panic was reproduced + root-caused to the missing exemption; this is the fix.
- R3: InstallVerbs remains the single source for the pure-install set.
- R7: consumer boot + fixture validate + negative-rule output pasted above; spec gates pasted.
- R6: branch-per-change PR, no force-push.
- R10: the disposable-bed proof of the full config: path lands with the distro-cachyos bed R10 (check-crabbox-e2e-pod); this PR's consumer proof is the fresh-rebuild binary boot + live box validate above.
- R2/R4/R5/R8/R9: N/A — additive one-line map entry; no cutover/deletions/artifacts/binary in this repo.

## Change classification

- **Class:** contract metadata (spec). **Verification gate:** consumer fresh-rebuild boot + live validate pasted + spec gates green. **Attribution:** fully tested and validated (consumer-side runtime path executed on a fresh rebuild).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
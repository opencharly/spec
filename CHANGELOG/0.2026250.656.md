# 0.2026250.656 — fix(schema): comments keep the current sdk/cli names until the CC-2 rename PRs land (R1/B5)

## Summary

Follow-up to the merged spec #111 (the fleet vocabulary retirement — FleetNode → DeployNode in the SPEC code symbols). #111 also renamed some COMMENT lines that reference sdk and CLI surfaces — but those surfaces are still named with the `fleet` vocabulary in their own repos (the sdk `deploykit.LoadFleetConfig`/`SaveFleetConfig` functions and the `charly fleet add/del/from-box/export` CLI verbs are the CURRENT, existing names; their renames to `deploy` terms ride the CC-2 wave's later sdk and charly PRs). Per R1/B5 — comments must describe what exists today — this PR reverts those comment lines to the current `fleet` names. The spec-internal rename (#111: `FleetNode` → `DeployNode`, `fleet/` → `deploy/`, `flattenFleetVenues` → `flattenVenuesByPosition`) is untouched.

## How tested (pasted, LIVE)

```
$ go build ./...   → EXIT 0
$ go vet ./...     → EXIT 0
$ go test -count=1 ./...  → 18 packages ok, 0 FAIL
$ git grep -n "LoadDeployConfig" -- schema/seam.cue spec/cue_types_gen.go  → 0 matches (reverted to LoadFleetConfig)
$ git grep -n "charly deploy add" -- .  → 0 matches (reverted to charly fleet add)
```

## Rulebook compliance

- R0: AGENTS.md read; git-workflow discipline.
- R1: the comment divergence (forward-references to not-yet-renamed sdk/cli names) was flagged by the validator on the #113 duplicate; reverted to the current names.
- R2: no deferred defect — the renames are the CC-2 wave's own subsequent PRs (named in the plan), not parked.
- R3: one naming vocabulary; comments describe current reality; the rename proceeds wave-ordered.
- R4: no workarounds; no aliases.
- R5: this PR re-reverts the forward-references; the spec-internal rename stays.
- R6: PR-only; no force-push; no direct main push.
- R7: the full gate suite + the executed sweep pasted above.
- R8: N/A — no runtime service change.
- R9: N/A — CalVer pins unchanged.
- R10: the suite ran on the committed tree (fdb68e1); the executed sweeps prove the reversion.

## Change classification

Attribution: **documentation reviewed** (comment-only change — the A3 ceiling; no code path to execute).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*
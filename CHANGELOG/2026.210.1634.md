# 2026.210.1634 — ExtraCandyRefs on #BuildRequest (#55 STEP 3-II, producer leg)

STEP 3-II of the everything-is-a-plugin import-purity plan adds an OPTIONAL wire
field to the build-resolve request so the super side's `build:generate` no-write
resolve Op can widen `resolveBuildEngine` with an overlay's `add_candy` set —
mirroring the pre-existing `#ResolveProjectRequest.ExtraCandyRefs`, which already
widens the project-resolve path the same way. This is the PRODUCER leg; the
superproject `require` bump + the resolve call-site wiring ride their own
follow-on PR in the B2 dependency order.

New CUE-sourced field (SDD — a wire data-carrier field is CUE-sourced; the
`#ResolveProjectRequest.ExtraCandyRefs` precedent, hand-writing a wire struct is
forbidden):

- `#BuildRequest.extra_candy_refs?: [...string] @go(ExtraCandyRefs)`
  (`schema/buildwire.cue`) — extra candy refs to widen the build resolve (e.g. an
  overlay's `add_candy` set), scanned into `resolveBuildEngine`. OPTIONAL (`?`) →
  the generated Go carries `json:"extra_candy_refs,omitempty"`.

Backward-compatible, purely additive: no existing field renamed or removed; every
prior `#BuildRequest` producer/consumer round-trips unchanged. The only touched
files are `schema/buildwire.cue` (the CUE source) and the regenerated
`spec/cue_types_gen.go`.

Verification: `go build/vet/test ./...` green (standalone, `GOWORK=off`);
`gofmt -l .` empty; the SDD reproducibility gate `TestGenReproducible` passes and
a fresh `task cue:gen` (pinned cue) produces zero diff.

Assisted-by: Claude Code Anthropic Claude Opus 4.8 (analysed on a live system)

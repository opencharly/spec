# 2026.215.1928 — refactor(spec)!: #55 import-purity — capability/matcher/value-type relocations + testkit + exec-hang fix

Five accumulated legs of the #55 program's spec producer line (bedcec9→head):

- **fix(exec)**: stdout/stderr pipe deadlock in the Wait-monitor regression test — concurrent
  drain (sync.WaitGroup) + a 30s bound on the one unbounded real-subprocess test; R1-RCA'd
  from a live 600s hang (/proc evidence: anon_pipe_write on a 64KiB stderr backlog), proven
  clean 100x + under -race.
- **feat(testkit)**: CopyCandyFixReplace — the shared candy-fixture stager for out-of-process
  plugin e2e tests (closed charly's last sdk string-literal files).
- **feat(spec) value types**: BuildResolvedBox, BundleConfig (+Lookup/LookupKey),
  DeployRecord/CandyRecord/StepRecord, LedgerSchemaVersion, CloudInitRuntimeParams — all
  all-fields-already-spec moves, purely additive; sdk aliases back (its leg).
- **test**: the LocalPkgInstallStep IR-contract test relocated from charly.
- **feat(spec/capability)**: ProvidedCapability + StepContract + CLISubcommand into the
  cuelang-free spec/capability slice; MatchAll + matcher logic into spec/spec beside
  spec.Matcher (the clireflect-pattern mini-B2; spec/climodel keeps cuelang only for
  ValidateGenerated via a CLISubcommand re-export).
- **docs**: stale kernelFloor/residueOwner comment sweep (the floor-gate rewrite's R5).

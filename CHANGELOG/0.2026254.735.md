# 0.2026254.735 — docs-config: correct the go_mod_path default (charly/go.mod, not charly/charly/go.mod)

## Summary

One-line correction of the `#DocsConfig` default: `go_mod_path?: *"charly/charly/go.mod"` → `*"charly/go.mod"`. The charly repo's module directory is `<repo>/charly/`, so the go.mod lives at `charly/go.mod`; the previous default made config-driven generation abort with ENOENT when a consumer relied on the default (discovered live during the docs regeneration landing; the charly config node fixes its explicit value in a sibling PR).

## How tested

From the pushed tree (branch feat/docs-config-path @ f89bec60d75955d162cb45fa18ece2bf2e72b1d3):
- `task cue:gen` run twice — both exit 0, second run produces no artifact drift (deterministic no-op; gengotypes emits GoModPath as a plain string field and does not embed schema defaults, so the committed diff is exactly the schema source).
- `go test ./schema/ ./spec/ -run 'DocsConfig|DocsNode|DocsKind'` — PASS (round-trip, rejects-unknown/wrong-types); `go test ./...` — 18/18 packages ok; `gofmt -l` empty; `go vet ./...` clean.

## Rulebook compliance

- R1 — ENOENT RCA'd to the one-segment-deep default; fixed at the contract and at the consumer config (sibling charly PR); no workaround.
- R3 — single canonical default; both surfaces align to the same corrected value.
- R6/Git — no force-push, no direct-main push, single-purpose change.

## Change classification

- **Change class:** CUE schema default correction (contract).
- **Verification gate:** cue:gen determinism + the docs config test package + full module tests.
- **Attribution tier:** analysed on a live system — the failing default was reproduced in the consuming generation run and corrected at both layers.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
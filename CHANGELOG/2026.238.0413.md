# 2026.238.0413 — fix: ExternalBuilderPluginRef returns the standalone plugin-candy ref (the candy cutover Phase 4)

## Summary

The candy de-submodule cutover, Phase 4: `ExternalBuilderPluginRef` now returns the STANDALONE plugin-candy ref (`@github.com/opencharly/plugin-builder-<word>/candy/<name>`) instead of the pre-cutover in-repo path (`@github.com/opencharly/charly/candy/plugin-builder-<word>`). The in-repo candy dirs are deleted in Phase 4, so the old ref makes the builder-connect fetch hang (the charly parity test's 10m timeout — RCA'd). The connect path resolves the standalone ref through the same fetch the runtime uses.

## How tested

**The new failing-without-it test passes:**

```
$ go test ./spec/ -run "TestExternalBuilderPluginRef" -count=1 -v
--- PASS: TestExternalBuilderPluginRef_StandaloneRef (0.00s)
ok  	github.com/opencharly/spec/spec	0.002s
```

**The module builds:**

```
$ go build ./spec/
(exit 0)
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — Go mechanism change |
| R1 | The parity-test 10m timeout was RCA'd: the stale in-repo builder ref made the connect fetch hang; this fix resolves it |
| R2 | No deferral — the ref shape lands here |
| R3 | ONE ref source (the externalBuilderPlugins map) |
| R4 | No workaround — the standalone ref is the runtime's fetch target |
| R5 | The old in-repo ref is replaced by the standalone ref |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Test + build output pasted above |
| R10 | The failing-without-it test pins the new shape |

## Change classification

- **Change class**: mechanism (externalized-builder ref shape, Phase 4)
- **Verification gate**: the new test + the module build + the org validator
- **Attribution tier**: `analysed on a live system` — the ref shape is pinned by the new test; the runtime connect path is exercised by the charly parity test once the pin chain lands

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
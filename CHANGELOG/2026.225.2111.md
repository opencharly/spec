# 2026.225.2111 — helm_charts on the kubernetes deploy substrate

The first repo of the Unit 1 helm cutover (spec → main): the `kubernetes:` deploy substrate gains a `helm_charts:` field so the kustomize/helm venues install chart releases alongside the generated workload. The k8sgen emitter (main-repo consumer) translates each entry into a kustomize `helmCharts:` overlay entry; the vm/pod/local venues install the same releases via `step:helm-release` (the new external step word). Field set confirmed by the Unit 1 schema spike.

- **`schema/deploy.cue`**: `helm_charts?: [...#HelmChart]` on `#KubernetesDeploy` + the `#HelmChart` def (`repo?`, `chart!`, `version?`, `release!`, `namespace?`, `values_files?`), CUE-sourced per SDD.
- **`spec/cue_types_gen.go`**: regenerated via `task cue:gen` (reproducible — a second run is a no-op).

Placeholder CalVer — the pr-validator finalizes it at merge.

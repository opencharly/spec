# 0.2026250.929 — test: dedupe the stage/parallel schema table-runner (fix main golangci-lint dupl)

## Summary

The repo's `ci` check has been RED on main since the CC3 stage/parallel merge: golangci-lint `dupl` flags `spec/stage_parallel_cue_test.go:52-74` vs `94-116` — the two schema table-runners (stage on `#Step`, parallel on `#DeployValue`) carry byte-identical bodies.

R3: extract one shared runner, `assertStepDefRejectAccept(t, schema, defPath, label, value, reject)`, and have both tests call it. Behavior preserved (same def lookup, unify, Concrete(false)+Final() validate, reject/accept messages now parameterized by label).

## How tested

- `golangci-lint run --allow-parallel-runners ./...` → `0 issues.` (the exact dupl failure gone).
- `go test ./spec/ -run "TestOpStage|TestDeployParallel" -count=1` → `ok github.com/opencharly/spec/spec`.

## Rulebook compliance

- R3: one implementation of the accept/reject runner; no duplicated block.
- R7: lint + targeted tests pasted above (full `go test ./...` also ran — see config-verb PR precedent; rerun gates here are the focused ones plus the module lint).

## Change classification

- **Class:** test refactor (unblocks main CI). **Verification gate:** golangci-lint `./...` 0 issues + spec tests green. **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
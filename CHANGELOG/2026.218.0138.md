# 2026.218.0138 — K-wave 2: the residue-ledger wire surface

The spec leg of the K-wave 2 residue-ledger program: the wire-type surface for the
relocated seams. The program (super CHANGELOG 2026.218.0000) drove charly/ core to
the kernel/plugin boundary law's end state, measured by the per-file residue ledger
(56/56 satisfied, tools/ledger-validate PASS).

- The config-resolve wire types (#ConfigResolveRequest/#ConfigResolveReply) deleted
  with the seam; the resolve-target-add helpers (PlansFromViews, ReconstructParentExec,
  EmitOptsFromResolveTargetAdd) relocated.
- The feature seam wire types (#FeatureRequest/#FeatureReply) deleted; #FeatureEntity
  kept.
- The deploy-from-box wire types deleted with the seam.
- #PodUpdatePayload.tree_json dropped — the node rides #PodLifecycleRequest.node.
- The arbiter wire surface adjusted as the bracket seam died.
- CLISubcommand carries `hidden` — the hidden-but-reachable wire flag for the
  run-local harness re-exec (F-CLI-NEST).
- The UnifiedDeployTarget/LifecycleTarget kind-agnostic contracts relocated from
  charly core (plain Go, no aliases).

All wire types CUE-sourced (task cue:gen / wire:gen); regeneration on a clean tree
is a no-op.

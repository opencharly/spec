# 0.2026251.500 — F2: bridge-deletion seam surface — CandyIsImage/BuildCandy/CueDocFromJSON/ParseDocStream + shared set-body folds

## Summary

Parser-consolidation F2 wave (plan/nested-capture-architecture.md, Cutover F2 — the genericNode dual representation dies) — the **spec slice**: the seam surface the charly host consumes plus the shared set-body folds.

1. **ProjectLoader gains the four parsed-node seam methods** the charly side of the wave uses to delete the genericNode bridge:
   - `CandyIsImage(pn ParsedNode) bool` — the candy box⊻layer routing restated on the parsed node (JSON-body scan for `base`/`from`; the deleted genericNode yaml-key scan's parsed-node twin).
   - `BuildCandy(pn ParsedNode) (string, *InlineCandy, error)` — the parsed-node candy constructor (decode via `DecodeNodeValue(pn)`; node-key name stamping).
   - `CueDocFromJSON(path string, data []byte) (cue.Value, error)` — the canonical-JSON CUE ingest (the validateKindValueCUE gate's deleted gn→yaml.Marshal→CueDocFromYAML round trip becomes a direct JSON ingest).
   - `ParseDocStream(data, srcLabel, srcDir string, seams WalkSeams)` — the ONE doc-stream composer both the file walk and the embedded default-vocabulary stream drive.
2. **SetBoxInto/SetCandyInto** — the shared set-body fold `Config.SetBox`, `UnifiedFile.SetBox/SetCandy` AND the `foldCandyKind` dispatch all perform (R3: one fold, no inline copies); the box⊻layer factory writes `acc.Box`/`acc.Candy` through the same helper the typed map accessors use, and its inline set bodies die with the bridge.

Implementations land in sdk/loaderkit (sdk PR) and the loader plugin (plugin-loader PR) of the same wave; the charly slice deletes `node_parsed.go`/`node_parse.go`/`node_candy.go`/`node_build.go` and rewrites the callers.

## How tested

All commands on the final committed tree (branch `feat/f2-bridge-deletion`, no replaces), `-count=1`:

```
$ gofmt -l .            # empty (nothing printed)
$ go build ./...        # exit 0
$ go vet ./...          # exit 0
$ go test ./... -count=1
ok  github.com/opencharly/spec/climodel  0.058s
ok  github.com/opencharly/spec/container  3.325s
... (18 packages, all ok; spec 0.631s) ...
ok  github.com/opencharly/spec/transport  0.218s
```

New coverage — the shared set fold:

```
$ go test ./spec/ -run TestSetFolds_NilAllocAndRoundTrip -count=1 -v
--- PASS: TestSetFolds_NilAllocAndRoundTrip (0.00s)
PASS
ok  github.com/opencharly/spec/spec  0.003s
```

## Rulebook compliance

- R3 — SetBoxInto/SetCandyInto are the ONE set-body fold (Config.SetBox / UnifiedFile.SetBox/SetCandy / foldCandyKind share it; no inline copies).
- R5 — the new seam methods are additive here; the deleted-identifier sweep for the genericNode bridge lands with the charly slice of the wave (node_parsed.go/node_candy.go).
- R6 — branch-only (feat/f2-bridge-deletion), no force-push, no hook bypass. R10 — all gates on the final committed tree.

## Change classification

- Change class: contract extension + refactor (parser-consolidation wave F2)
- Verification gate: gofmt/build/vet + full spec test suite green on the final committed tree
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
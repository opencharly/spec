# 0.2026249.910 — feat(schema): from_snapshot on CheckBedReply + CheckBedMember (the vm-build step threads it)

## Summary

Follow-up to spec #99 (the from: name:tag schema): **CheckBedReply.from_snapshot + CheckBedMember.from_snapshot** — the runner's vm-build step threads the deploy's unified snapshot to `charly vm build <entity> --from-snapshot <tag>` (both the bed root and group members). Plus the ttl-param unparam suppression (the pre-existing lint break git_client.go's gofmt fix surfaced).

## How tested

```
$ task cue:gen   # idempotent
$ go build ./...
$ go test ./spec/ -run 'TestVmBuildRequestFromSnapshotRoundTrip|TestCheckBedReplyFromSnapshotRoundTrip|TestCheckBedMemberFromSnapshotRoundTrip' -v
--- PASS: TestVmBuildRequestFromSnapshotRoundTrip
--- PASS: TestCheckBedReplyFromSnapshotRoundTrip
--- PASS: TestCheckBedMemberFromSnapshotRoundTrip
$ gofmt -l .    # clean
```

All three round-trip tests FAIL without their schema field (the yaml tag silently drops it) — each field's R10 gate.

## Rulebook compliance

- R0: git-workflow + strict-policy skills loaded first.
- R1: zero new warnings; the unparam is the pre-existing break surfaced by #99's gofmt fix (WHY documented in the nolint).
- R2: this is the named Phase 3 batch; plugin-vm consumes VmBuildRequest.from_snapshot (#24), plugin-check consumes these two reply fields (follow-up).
- R3: the fields mirror CheckBedReply's existing vm_template / CheckBedMember's existing from — no duplicated spelling.
- R4: no workarounds; the nolint carries the design WHY.
- R5: additive — the retirement (source.kind: clone) stays plugin-vm's PR.
- R10: all THREE from_snapshot fields now have round-trip tests.

## Change classification

Class: schema (additive) + lint suppression. Gate: cue:gen + three round-trip unit tests (each fails without its field). Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
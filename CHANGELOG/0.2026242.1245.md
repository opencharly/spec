# 0.2026242.1245 — fix(container): drop the unused shortName parameter on electResolvedImage

## Summary

`main` is red, and this fixes it.

#82 split `ResolveLocalImage` into a candidate gather and an election. The election kept a `shortName` parameter it never reads — all the name filtering happens in `gatherResolverCandidates`, before election ever runs.

```
container/local_image_coneb.go:505:82: electResolvedImage - shortName is unused (unparam)
```

## The gate caught its own author, one commit later

This was found by the `golangci-lint` step added in #78 — landed minutes earlier, and its first catch is my own regression. That is the argument for the gate, demonstrated rather than asserted: without it, this sits on `main` until it blocks whoever touches this repo next, because the pre-commit hook lints the **whole repo** rather than the diff.

It is also a straightforward process failure on my part. Before #82 I ran `go build ./container/`, `go vet ./container/` and `go test ./container/` — none of which see `unparam` — and not `golangci-lint run ./...`, the one check that would have.

## How tested

```
$ go build ./...
build exit=0

$ golangci-lint run ./...
0 issues.

$ go test ./...
packages ok: 18   (no failures)
```

`golangci-lint run ./...` is the check that was failing; it is clean on this tree, and this PR's own `go` job is the gate confirming it.

## Rulebook compliance

- **R1 (RCA)** — the finding is quoted from the failing run, and the reason it escaped (the wrong subset of checks run locally) is stated rather than left implicit.
- **R2 (no deferral)** — `main` is red now; fixed here, not filed.
- **R4 (no workarounds)** — the dead parameter is removed, not silenced with a `//nolint` or an `_ = shortName`.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass.
- **R7 (prove the gate)** — the exact failing command run locally and pasted, plus this PR's own CI.

## Change classification

- **Change class:** code change (one unused parameter removed from an unexported function)
- **Verification gate:** `golangci-lint run ./...`, `go build ./...`, full `go test ./...` on the committed tree
- **Attribution tier:** `fully tested and validated` — the failing check passes locally on this tree and the full suite is green

*Assisted-by: Claude Code Anthropic Opus 5 (fully tested and validated)*
# 2026.218.0000 — rename Bundle→Fleet across the spec module

The deploy-set concept is renamed to Fleet across the spec module, following the
superproject's canonical README glossary. `BundleConfig`→`FleetConfig`,
`BundleNode`→`FleetNode`, the `Bundle` map field→`Fleet`, `bundle_keys.go`→
`fleet_keys.go`, `deploy_bundle_ops.go`→`deploy_fleet_ops.go`, and every comment
reference. The on-disk config key stays `deploy:` — zero config-format change.
Generated cue_types_gen.go/vocab_gen.go/version_gen.go and the proto wire stubs
regenerated from the renamed schema sources. Placeholder CalVer — the
pr-validator finalizes it at merge.

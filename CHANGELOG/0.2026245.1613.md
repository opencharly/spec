# 0.2026245.1613 — chore(gengotypes): regenerate Deploy.Record wrap field (cue:gen after spec #88)

## Summary

Regenerates the spec gengotypes for the deploy record: field that opencharly/spec #88 (feat(schema): deploy record: field — whole-run recording wrap, G5 batch) added to the CUE schema. #88 shipped only the schema + a cuecontext-level B12 test; the checked-in gengotypes were never regenerated, so the Go runtime type Deploy lacks the Record field. Consequence: box validate passes (CUE path) while the Go loader silently drops record:, so the check-runner NodeJSON never carries the field and the whole-run recording wrap hook cannot fire.

This PR is the missing codegen half of the SAME G5 batch as #88 (the deploy record: whole-run wrap): the runner seam + bed R10 land in opencharly/plugin-check's feat/bed-record-wrap work (the G5 batch's runner step), which depends on this field existing in the Go types.

Diff: one generated file, +27 lines — task cue:gen output (TestGenReproducible guards the regen contract).

## How tested

```
$ cd spec && task cue:gen && go build ./spec/...
(no output — clean)

$ go test ./spec/ -run 'TestDeployRecordWrap|TestGenReproducible' -count=1
ok  	github.com/opencharly/spec/spec	0.270s

$ git diff origin/main --stat
 spec/cue_types_gen.go | 27 +++++++++++++++++++++++++++
 1 file changed, 27 insertions(+)
```

## Rulebook compliance

- R3: gengotypes are generated from the single schema concatenation (no hand-edits; the diff is exactly what task cue:gen emits)
- The consuming runner + its bed R10 are the G5 batch's runner step in opencharly/plugin-check (feat/bed-record-wrap), which lands after this codegen ships

## Change classification

- change_class: contract/module (spec gengotypes codegen)
- verification_gate: go build + go test + reproducible regen pass locally
- attribution_tier: analysed on a live system (regen + tests run locally; the field's RUNTIME behavior is verified by the G5 runner R10 in plugin-check, not by this codegen PR)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
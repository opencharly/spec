# 0.2026249.855 — feat(schema): from_snapshot on VmBuildRequest + document the unified from: name:tag spelling

## Summary

Phase 3 (the from: name:tag functional half) schema pieces:
1. **#VmBuildRequest.from_snapshot** — the `charly vm build --from-snapshot <tag>` flag envelope (builds the entity as a clone of its own golden at that snapshot).
2. **deploy.cue docs** — the unified NAME:TAG spelling: `from: base:golden` == `from: base` + `from_snapshot: golden`; split on the last `:` (a VM entity name may not contain `:`); ImageBacked (pod/kubernetes) refs are explicitly NOT split (image tags contain `:`).

The deploy node's existing from_snapshot field is unchanged. The loader split (opencharly/sdk #217) consumes the same spelling. Plus a mechanical `gofmt -w refs/git_client.go` (a pre-existing unformatted file on origin/main broke EVERY PR's gofmt gate — reproduced with `git show origin/main:refs/git_client.go | gofmt -l`).

## How tested

```
$ task cue:gen
cue:gen: regenerated spec/{cue_types_gen.go,vocab_gen.go,version_gen.go}
$ go build ./...
$ go test ./spec/ -run TestVmBuildRequestFromSnapshotRoundTrip -v
marshalled: box: cachyos-vm
  from_snapshot: golden
--- PASS: TestVmBuildRequestFromSnapshotRoundTrip (0.00s)
$ go test ./exec/...
ok  	github.com/opencharly/spec/exec
$ gofmt -l .   # clean (git_client.go fixed)
```

The round-trip test FAILS without the schema field (the yaml tag silently drops it) — the R10 gate for the field.

## Rulebook compliance

- R0: read the git-workflow + strict-policy skills before this change; the schema area follows the repo's own task cue:gen discipline.
- R1: the gofmt break RCA'd above (a pre-existing main unformatted file, fixed mechanically in this PR); zero new warnings in the pasted output.
- R2: the plugin-vm retirement (source.kind: clone entity arm) is Cutover A addendum Phase 3, task 3 — this schema PR is the batched first half (loadable without the plugin change); the second half (plugin-vm consuming VmBuildRequest.from_snapshot) rides the same named batch.
- R3: the from_snapshot field is the deploy node's EXISTING field (schema/deploy.cue:185) — the build-request half is the only addition; no duplicated spelling.
- R4: no workarounds — additive schema, no renames.
- R5: the OLD entity-arm syntax (source.kind: clone) is retired in plugin-vm's PR, pointed at by a hard error — this PR only ADDS the deploy-side spelling.
- R10: the round-trip unit test above proves the new field (fails without it).

## Change classification

Class: schema (additive) + one mechanical gofmt fix. Gate: cue:gen idempotence + build + the new round-trip unit test. Attribution: fully tested and validated (the new field's behavior is unit-proven).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
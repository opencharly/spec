# 0.2026245.1517 — feat(schema): deploy record: field — whole-run recording wrap

## Summary

Adds the `record:` field to the #Deploy node — the whole-run recording wrap (G5 batch): when a deploy node sets `record: {terminal|desktop, fps, record_env, artifact…}`, the check-run runner (plugin-check, follow-up PR) records the live phases and lands the artifact in the run evidence (`recordings.yml` beside summary.yml — the manifest itself is already merged via plugin-check #17).

**Changes:** `schema/deploy.cue` +18 (the `record?: #RecordWrap` field + the `#RecordWrap` def) and `spec/deploy_record_test.go` (+50) — B12 coverage: the compiled-schema test accepts valid wraps and rejects invalid ones, FAILING if the field/def is absent.

## How tested

```
$ go test ./spec/ -run TestDeployRecordWrap -v
--- PASS: TestDeployRecordWrap (0.01s)
    --- PASS: .../record_terminal_wrap_accepted
    --- PASS: .../record_desktop_wrap_accepted
    --- PASS: .../record_fps_below_bound_rejected
    --- PASS: .../record_unknown_field_rejected
PASS   ok  github.com/opencharly/spec/spec  0.014s
```

The test compiles the SAME schema concatenation the runtime uses (`schemaconcat.ConcatSchema`) and validates concrete values against `#Deploy` — the schema path ships with its own coverage in THIS PR (the schema-only change cannot run a bed; the consuming runner's bed R10 lands in the plugin-check follow-up with the charly pin bump).

## Rulebook compliance

- R1: change via PR to the owning repo (opencharly/spec). R3: one wrap def; the test uses the single concat contract (schemaconcat — R3). R4: no workarounds. R5: N/A — additive, no legacy deleted. SDD: CUE-first def; the Go shape regenerates in the consumer (charly pin bump — stated). B16: the capability (recording runner) correctly lives in plugin-check, not core.

## Change classification

- **Change class:** schema addition (+ B12 schema test)
- **Verification gate:** the compiled-schema validation test green (fails-without-the-field)
- **Attribution tier:** syntax check only (static schema + validation test; the consuming live path is the follow-up's R10)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (syntax check only)*
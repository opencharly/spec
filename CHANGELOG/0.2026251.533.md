# 0.2026251.533 — F2.6: reserve the verb_primaries document directive (platform-verb primary data)

## Summary

Parser-consolidation F2, item 6 (plan/nested-capture-architecture.md) — **re-landing the `verb_primaries` document-directive reservation** that the F2 wave's spec PR #123 lost to the validator's auto-merge race (PR #123's squash covered only its first commit; this change was its second commit, now re-landed as its own PR).

`verb_primaries:` joins the reserved #NodeDoc directives (the `providers:`/`compiled_plugins:` family): verb word → primary input field, declared in the binary-embedded charly.yml. It is the compiled-in DATA source charly's init() seeds the parse-time desugar table from (F2.6 — the frozen 11-entry table dies, parse-time determinism preserved by data). Recognized as a directive — never mis-read as an entity — by the Go directive set (spec.DocDirectives, regenerated vocab) and admitted by the #NodeDoc closedness def.

## How tested

All commands on the final committed tree (branch `feat/f2-verb-primaries`):

```
$ task cue:gen
cue:gen: regenerated spec/{cue_types_gen.go,vocab_gen.go,version_gen.go}
$ git diff --stat
 schema/node.cue   | 10 ++++++++++
 spec/vocab_gen.go |  1 +
 2 files changed, 11 insertions(+)
$ go build ./...    # exit 0
$ go test ./spec/ -count=1
ok  github.com/opencharly/spec/spec  0.503s
$ go test ./... -count=1   # full suite
ok  github.com/opencharly/spec/...   (all packages)
```

The consumer chain is proven end-to-end in the charly slice of the wave (embedded charly.yml `verb_primaries:` + init seeding + node_desugar_test.go parity assertion; the full charly kernel suite — incl. the corpus tests that exercise `kube:`/`cdp:` scalar sugar across the real repo corpus — is green on that tree).

## Rulebook compliance

- R3 — one reserved-directive vocabulary (spec.DocDirectives + #NodeDoc) admits the new data key; no parallel list.
- R6 — branch-only (`feat/f2-verb-primaries`), no force-push, no hook bypass. R10 — gates on the final committed tree.

## Change classification

- Change class: schema vocabulary extension (parser-consolidation wave F2)
- Verification gate: cue:gen reproducible + gofmt/build/vet + full spec test suite green
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*